# shulun 项目操作守则

本项目是数学研究（矛盾场专著、Phi-LPF 路线、奇偶性屏障攻关），单次会话历史上吃过 $298。这份守则用来防止重复烧钱。

## 〇、用户画像与回答风格（最重要！）

**本项目主要用户不是写代码出身**，是数学方向的研究者/合作者，通过飞书 bot 跟我交互。他们：
- 不擅长读 diff、不熟 git、不会读 stack trace
- 看不懂 "PR #123 merged"、"refactor to use Strategy pattern"
- 希望用**人话**解释做了什么、为什么、下一步该怎么走

**因此本项目不适用我全局 CLAUDE.md 里"默认只给结论，不展开"的规则。** 这里反过来：

- **多解释一句**：每个数学/技术动作配一句"这一步对应数学上 X，目的是 Y"
- **拆步骤说**：复杂改动用编号列出 1. 2. 3.，不要只丢一段总结
- **复述用户问题**：用一句话确认"你想要 X 对吧"再开始做，避免误解后白做
- **慢就慢点**：宁可多想几秒（extended thinking 已开）也不要快但浅
- **比较 codex 风格**：如果用户切自 codex（recap hook 会注入背景），延续 codex 的解释颗粒度，不要突然变简洁


## 一、会话节奏（最重要）

### 硬上限

- **单 session 不超过 6 轮 user-assistant 往返**。到上限就让用户 `/clear`。
- **单 session 不超过 90 分钟**。跨度过长 prompt cache（5 分钟 TTL）会反复重建。
- 每 6 轮在回复末尾主动提醒："建议 `/clear` 后开新 session，把本轮结论压成 1 段背景传入。"

### 拆分原则

每个"突破方向"独立 session，不要在同一会话里横跳：
- 方阵斜线覆盖 / CRT 周期镜像 / Phi 递推 / P 阶递降 / 奇偶性屏障 …
- 每条线一个 session，互不污染上下文。

## 二、复读机检测（防止 14 条"继续突破"那种事）

如果用户的本轮 prompt 与最近 3 轮 prompt **语义高度相似**（连续"继续努力突破"、"继续融合黎曼高斯欧拉智慧"等），**不要直接照做**。先回：

> 我注意到你这是第 N 次发类似指令。重复 prompt 不会让证明前进，反而每轮多烧上下文。请改成以下之一：
> 1. **给新约束**（限定方向、排除某条路线、引入特定外部定理）
> 2. **提具体子问题**（像之前问 "Phi-LPF 能否闭合区间端点差值" 或 "BHP 在 X=P² 能否降到 0.5"）
> 3. **`/clear` 后明确说要攻哪一条线**

只在用户给出新约束 / 新子问题后才继续推进。

## 三、模型路由

- **默认 Sonnet**。日常阅读论文、调试脚本、归档主稿、写证书 LaTeX、提交 Git，都用 Sonnet。
- **只在以下三种情况切 Opus**：
  1. 关键证明步骤的逻辑闭合论证
  2. 多条路线的取舍/取证策略
  3. 用户明确说"用 Opus / 深度推理"
- 模型切换前先告知一句："这一轮切到 Opus，因为 X。"

参考：单价 Opus ≈ 5× Sonnet，shulun 那次全程 Opus → 同样工作量 Sonnet 大概 $60 而不是 $298。

## 四、输出纪律

数学项目特别容易输出膨胀。守则：

1. **不要重印已经在 docs/ 里的定义和引理**。引用文件路径 + 节号即可（如 `docs/main.tex L1234`）。
2. **不要复述上一轮的结论**。直接说"基于上一轮 K3-trivial 闭合"即可。
3. **证书脚本的输出**只贴关键数字行，不贴全部 stdout。
4. **LaTeX 编译日志**只看 error/warning 首行。
5. 每轮结束给 1 段 ≤5 行的 "本轮净增量"：新引理 / 新证书 / 新提交 hash。

## 五、子代理 & 搜索

- 大范围找代码、找定理用 **Explore subagent** 或 **Grep**，不要把 ripgrep 全文塞进主上下文。
- 跨文件 audit 用 subagent，让它在子上下文里做完再回报结论。

## 六、Git & 提交

- 每条独立结论一次 atomic commit，commit message 用一句中文总结（项目已有的风格）。
- 不要等会话结束统一提交：每个证书 / 每个 audit 写完即提交，方便 `/clear` 后下次能从 git log 接续。
- 提交前用 `git diff --check` 校验，不上传编译副产物（.aux/.log/.fdb_latexmk 等）。

## 七、记忆 & 上下文

- claude-mem 已装。**搜历史结论用 `mem-search`**，不要让模型重新读 docs 全文。
- 新 session 开场让用户用 1 段背景说"上次做到哪 / 当前要攻哪条线"，不要靠模型自己重读全部历史。

## 八、紧急熔断

如果检测到本会话已超 50 轮或运行已超 4 小时，**主动拒绝继续**：

> 本会话已经过长（X 轮 / Y 小时），按守则必须 `/clear`。请把当前进展压成 ≤200 字背景，新会话继续。

---

**提醒**：这份守则的目的不是限制你做事，是防止过去那种"14 条复读式催更 + 全程 Opus → $298 一夜"的事再发生。用户立项时明确写过"审核是否有循环论证浪费时间"——这条守则就是落地这个目标的工具。
