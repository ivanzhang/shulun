# Prime Matrix 早期零行假设的矛盾矩阵路由器

**状态：** `early_zero_forced_into_named_contradiction_matrix_unconditional_closure_open`

假设早期零行目前已经被所有已建模型夹到命名终端里：它不再能作为自由覆盖现象存在。但现有成果还没有给出无条件矛盾；真正未闭合的是 anchor-collar 短素数纤维容量、早期复合 cofactor 递归下降、LocalSurvivor/SAE 与 primitive PDEC 容量排斥。

```text
direct_unconditional_contradiction_found=false
no_unnamed_exit_for_early_zero=true
row_column_unconditional_closed=false
strongest_current_frontier=AnchorCollarPrimeFiberCapacityBoundOrPDECReturn
```

## 1. 矛盾矩阵

| existing result | forced by early zero | closed fact | contradiction if | status | remaining |
| --- | --- | --- | --- | --- | --- |
| CLB 完成斜线屏障 | U_x=empty，因此 R_x=F_x。 | CLB-1/2 分解与素数洞判别已闭合。 | CLB-FillerBound: \|F_x\|<\|R_x\|。 | `conditional_contradiction` | 全局 \|F_x\|<\|R_x\| 仍未证明。 |
| 近邻零行/镜像路线 | 早期零行会给 CRT 周期内短首尾镜像间隔。 | 直接镜像矛盾已被否定；保留路线是 BoundaryPhaseNoncoverageOrStableRecurrencePDEC。 | 若能证明稳定短复现必排斥，或边界相位缺陷必排斥。 | `direct_contradiction_rejected_named_return` | 不能再把镜像对称单独当最终矛盾。 |
| 同 formal unit 相位缺陷准入 | R_x=F_x 可登记为 Omega=R_x, tau(c)=q(c), w(c)=1。 | EarlyZeroPhaseDefectSchemaAdmission 已闭合。 | EarlyZeroTerminalExclusionPackage 全部闭合。 | `schema_closed_terminal_exclusion_open` | EarlyZeroTerminalExclusionPackage |
| carry-shell 精确支撑 | 每个高补洞满足 h=a+b-floor(ab/P), c=ab mod P。 | ExactCarryShellIdentity 已闭合，底部带是 k=0 特例。 | CarryShellPrimitiveCapacityBound，或失败给 PDEC/SAE/ColumnCRT 回流。 | `support_geometry_closed_capacity_open` | CarryShellPrimitiveCapacityBoundOrPDECReturn |
| cofactor 深度门 | m 是 x-rough cofactor；x>=sqrt(P) 时 m 必为素数。 | SqrtGatePrimeCofactor 与复合 cofactor 递归回流已闭合。 | PrimePairCarryShellCapacityBound 与 CompositeCofactorDepthDescent 均闭合。 | `branch_split_closed_two_capacities_open` | PrimePairCarryShellCapacityAndCompositeCofactorDepthDescent |
| canonical anchor collar | 真双素分支中最小高素锚满足 x<q<sqrt((x+1)P)，每条 q-fiber 长度 <sqrt(P)。 | canonical_anchor_collar_closed 与 fiber_short_interval_reduction_closed 已闭合。 | AnchorCollarShortPrimeFiberUpperBound，或过载回流 PDEC/SAE/ColumnCRT。 | `strongest_current_narrow_frontier` | AnchorCollarPrimeFiberCapacityBoundOrPDECReturn |
| P 列锚与层叠轮 | 高补洞支付必须沿 P 列/圆柱相位转写为同一 payment/Phi 支撑。 | P-column anchor、layered wheel 和几何 payment 字母表已登记。 | DPRC/FixedWheel/NewLayer/FlatDLS 输入全部闭合，或早期零行支付过载生成 PDEC。 | `geometry_ledger_closed_signed_budget_open` | DPRCAlpha043CenteredDiscrepancyOrNamedLayerReturn AND SignedGeometricLedgerVariationBranchLiftAndReturn |
| PDEC/SAE/ColumnCRT 终端家族 | 若覆盖压力集中、孤立或固定列位移复用，必须分别进入 PDEC、SAE/LocalSurvivor、ColumnCRT 吸收。 | ColumnCRT 和 SAE 均不再是独立第四出口；PDEC 需要同 formal unit primitive schema。 | 所有准入后的 primitive PDEC 容量不等式和 LocalSurvivor/SAE 证书闭合。 | `no_unnamed_exit_terminal_exclusion_open` | Primitive/Persistent PDEC budget + LocalSurvivor/SAE + displacement PDEC 排斥。 |

## 2. 已形成的结构夹击

| front | early-zero pressure | structural conflict | closure need |
| --- | --- | --- | --- |
| CLB residual-filler front | 早期零行把 CLB 的剩余洞集合压成 R_x=F_x。 | 这与 CLB 的预期容量方向 \|F_x\|<\|R_x\| 正面冲突。 | 证明全局 CLB-FillerBound，或把等量补完解释为 PDEC/SAE/ColumnCRT。 |
| same-formal-unit front | 所有补洞必须登记在同一个 Omega=R_x、tau(c)=q(c)、w(c)=1 的 formal unit 中。 | 任意拼接不同窗口、不同 q 层或不同重复口径的补洞不能再计入同一证明账本。 | 关闭 EarlyZeroTerminalExclusionPackage。 |
| carry-shell front | 每个高补洞必须满足 h=a+b-floor(ab/P), c=ab mod P。 | 高素斜线不是自由扫过整行，而只能落在有限带进位壳；底部带还退化为 a+b=h。 | 证明 carry-shell 容量不足，或持久/稀疏/位移失败回流命名终端。 |
| cofactor-depth front | m=P-b 是 x-rough cofactor，且 x>=sqrt(P) 时必为素数。 | 复合 cofactor 只能躲在 x<sqrt(P) 的浅层递归壳里，不能作为大行段自由容量来源。 | 关闭 CompositeCofactorDepthDescentOrNamedReturn 与 EarlyBandLocalSurvivorOrSAEExclusion。 |
| anchor-collar short-fiber front | 真双素分支中最小锚满足 x<q<sqrt((x+1)P)，固定 q 后 m 的窗口长度 <sqrt(P)。 | 全行补完要求大量极短素数纤维协同满载；集中则不是随机补洞，而是 PDEC/SAE/ColumnCRT 证据。 | 当前最窄目标 AnchorCollarPrimeFiberCapacityBoundOrPDECReturn。 |
| mirror-stability front | 完整 CRT 周期镜像只给边界短间隔，不能自动给独立矛盾。 | 若强制短复现，则进入稳定位移缺陷；若无短复现，则进入边界相位非覆盖缺陷。 | 排斥 stable recurrence PDEC/ColumnCRT 或 boundary phase defect PDEC/SAE。 |
| P-column layered-wheel front | 补洞支付必须通过 P 列锚、圆柱相位和 layered wheel 的同一 Phi/payment 字母表转写。 | 斜线覆盖的几何自由度被列锚和层叠轮筛锁住；任何同步失败必须命名回流。 | DPRC/FixedWheel/NewLayer/FlatDLS 与 signed geometric ledger 仍需最终排斥。 |
| terminal no-fourth-exit front | 覆盖压力若集中、孤立或固定列位移复用，分别进入 PDEC、SAE/LocalSurvivor、ColumnCRT 吸收。 | 早期零行没有未命名逃逸口；剩余只可能是已登记终端未排斥。 | Primitive/Persistent PDEC budget + LocalSurvivor/SAE + displacement PDEC 排斥。 |

这些不是八个彼此独立的新假设，而是同一反例分支被连续压缩后的八个夹击面：
CLB 先把零行变成 `R_x=F_x`，formal unit 规定只能在同一账本计量，carry-shell 和 cofactor-depth 把补洞支撑压成有限壳，anchor collar 再把大行段压到极短素数纤维。
因此早期零行若存在，已经不是自由的斜线覆盖现象；它必须表现为短纤维满载、持久低模集中、孤立 survivor 或固定列位移复用。

## 3. 当前最强结论

早期零行假设已经与以下结构同时绑定：

```text
EarlyZeroRowWithinP
  => CLB residual equality R_x=F_x
  => same-formal-unit phase ledger
  => ExactCarryShellIdentity
  => CofactorDepth split
  => CanonicalAnchorCollar short prime fibers for x>=sqrt(P)
  => AnchorCollar capacity deficit OR PDEC/SAE/ColumnCRT named return
```

所以它已经和“任意斜线覆盖”直觉矛盾：高补洞必须同时满足同 formal unit、带进位壳、cofactor 深度、canonical collar 和短素数纤维五层刚性。
但这些刚性本身还没有自动给出 `R_x` 不可全覆盖的最终容量不等式。

## 4. 不能再作为最终矛盾的路线

- 真实样本中未见早期零行不能替代假设分支证明。
- 完整 CRT 镜像对称只给等价边界短间隔，不能单独推出矛盾。
- 从一个零行自动推出下一个零行很近没有已证机制。
- P^2±k 或层叠轮同余刚性只提供补洞通道限制，单独还不是全局容量排斥。

这些路线仍可作为路由器和证据来源，但不能在正文中直接写成无条件终局矛盾。

## 5. 剩余闭合包

要把上述命名夹击升级为无条件矛盾，还需要关闭：

```text
AnchorCollarShortPrimeFiberUpperBound
CompositeCofactorDepthDescentOrNamedReturn
EarlyBandLocalSurvivorOrSAEExclusion
PrimitivePDECBudgetForPersistentConcentration
DStructureTailLog4FiniteRankinFullLedgerIndependentAcceptance
```

当前最窄优先级仍是：

```text
AnchorCollarPrimeFiberCapacityBoundOrPDECReturn
```

也就是证明 anchor collar 中所有长度 `<sqrt(P)` 的短素数纤维总容量不能覆盖整个 `R_x`，或证明任何覆盖级过载都会进入已命名且可排斥的 PDEC/SAE/ColumnCRT。
