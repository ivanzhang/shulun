# Triad-A1 Generic WFD DI/BFI 外部合同路由器

**状态：** `generic_wfd_external_dibfi_contract_materialized_theorem_location_open`

generic noncanonical WFD 分支已经从一个含混的“外部 DI/BFI”压成精确外部合同：对象必须是未中心化原始 WFD；不能靠 SOURCE-CEN/BD-CEN 免费中心化；CRT 相位、well-factorable level、Type-I/II 范围、gcd/端点和平滑损失都已进入门控表。当前剩余不再是 canonical-source 内部数学链条，而是 DI/BFI 原文定理位置与假设逐项匹配。

## 1. 结构律

The remaining generic WFD branch is not a canonical-source support problem. Its legitimate external route is a direct original DI/BFI dispersion estimate for the uncentered WFD target. SOURCE-CEN/BD-CEN cannot be inserted as free centering. The available repository material already fixes the target object, phase normalization, well-factorable level interface, Type-I/II dyadic ranges, gcd/endpoint smoothing ledger, and log-loss absorption. The only remaining external-theorem obligation is to pin the precise DI/BFI theorem location and check every hypothesis against this target.

```text
canonical source branch:
  already handled by the internal RIW/Buchstab support chain;

generic noncanonical WFD branch:
  cannot borrow canonical support;
  must use direct original DI/BFI dispersion or return to PDEC/SAE;

direct original dispersion:
  estimate the uncentered WFD target itself;
  do not insert SOURCE-CEN/BD-CEN as a free identity;
  normalize CRT phase to standard Kloosterman form;
  match well-factorable level, Type-I/II ranges, gcd and endpoint budgets;
  then pin exact external theorem location and hypotheses.
```

## 2. 汇总

- `branch_statement_status=canonical_source_branch_statement_adopted_generic_wfd_external_only`。
- `closed_except_precise_external_citation=true`。
- `all_gates_closed=false`。
- `external_dibfi_contract_materialized=true`。
- `generic_wfd_self_contained_gap_closed=false`。
- `external_deep_theorem_version_status=contract_ready_pending_precise_dibfi_theorem_location_and_hypothesis_match`。
- `self_contained_version_status=open_for_generic_noncanonical_wfd_branch`。
- `next_external_target=DIBFIOriginalDispersionTheoremLocationAndHypothesisMatch`。
- `terminal_gap_after_router=DIBFIOriginalDispersionTheoremLocationAndHypothesisMatch`。

## 3. 对象二分表

| object | scope | route | status | can use this router |
| --- | --- | --- | --- | --- |
| `canonical RIW/Buchstab source branch` | lambda_c equals the canonical decision-tree sieve coefficient | internal support chain | `already_closed_in_source_lock_chain` | `false` |
| `generic noncanonical WFD branch` | lambda_c is only well-factorable; no canonical support lower bound is assumed | external DI/BFI original dispersion or PDEC/SAE return | `current_remaining_branch` | `true` |
| `uncentered WFD target` | the original KE-13/WFD-core linear Kloosterman window before block-centering | must be estimated directly by original dispersion, not by SOURCE-CEN | `target_locked_as_uncentered` | `true` |

## 4. DI/BFI 原始 dispersion 门控表

| gate | available | needed | gap | route | closed |
| --- | --- | --- | --- | --- | --- |
| `OriginalUncenteredWFDTargetMatch` | KZ-E spine identifies KE-13/WFD-core as the current generic WFD target | external theorem must estimate the uncentered original dispersion object | none at object-identification level | use original dispersion estimate, not a centered replacement | `true` |
| `NoCenteringShortcut` | SOURCE-CEN and BD-CEN audits block free local-variance subtraction | do not insert block centering unless the original dispersion identity provides it | none after routing: choose direct uncentered external estimate | direct DI/BFI original dispersion branch | `true` |
| `KloostermanPhaseNormalization` | KLS template converts CRT inverse phase to standard Kloosterman phase | match the generic WFD phase to DI/BFI inverse phase notation | none at template level | CRT phase normalization then dyadic Kloosterman window | `true` |
| `WellFactorableLevelMatch` | external index records BFI well-factorable weights; KZ-E uses well-factorable lambda_c | level and factorization ranges must be within BFI admissible ranges | exact theorem hypotheses still need page/theorem-level check | match lambda_c level to BFI dispersion theorem | `true` |
| `TypeIITwoVariableRangeMatch` | KZ-E spine records Type-I/II dispersion reduction; KLS template records C,S,H ranges | balanced two-variable blocks must match the quoted DI/BFI range | exact theorem hypotheses still need page/theorem-level check | dyadic Type-I/II block by block | `true` |
| `SmoothDyadicEndpointGcdBudget` | KLS template has gcd strata, dyadic, sawtooth and smoothing log-loss ledger | all endpoint and gcd costs must be absorbed before final log saving | none at ledger level | charge all costs to log^C and choose larger B(A) | `true` |
| `ArbitraryLogSavingAbsorption` | template chooses B(A)=A+C0+10 | external theorem must provide arbitrary log saving after loss absorption | depends on the precise cited external theorem statement | choose B(A) after dyadic/gcd/smoothing budgets | `true` |
| `ExternalCitationPreciseTheoremLocation` | bibliographic DI/BFI sources and functional adaptation are registered | exact theorem/proposition/page and hypothesis-by-hypothesis match | not yet pinned in the repository | audit original DI/BFI statements or quote a modern theorem with identical hypotheses | `false` |

## 5. 当前结论

本轮推进把最后外部缺口从一句话压成了一个单点可核查目标：

```text
DIBFIOriginalDispersionTheoremLocationAndHypothesisMatch
```

这仍不是 generic WFD 宽口径的完全自足证明；它说明除精确外部定理定位与假设匹配外，generic WFD 外部引用版所需的对象、相位和账本接口已经登记完成。
