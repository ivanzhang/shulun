# Prime Matrix strict nonterminal exact-UV fiber 非集中直攻路由器

**状态：** `strict_nonterminal_exact_uv_fiber_aperiodicity_reduced_to_absolute_mass_dispersion_open`

`NonterminalExactUVFiberAperiodicityEstimateForActualPreCauchySource` 继续被压缩为更精确的绝对质量命题：`PreTerminalExactUVFiberAbsoluteMassDispersionTheorem`。这不是换命题，而是排除 signed-only、seed-only、CRT/轮筛位置刚性和终端 packet 回流后的同一源熵核心。该绝对 fiber 分散定理尚未证明，行/列命题仍未无条件闭合。

```text
same_theorem_target_preserved=true
no_theorem_switch=true
nonterminal_fiber_aperiodicity_attack_closed=true
seed_only_formal_proof_blocked=true
signed_cancellation_only_rejected=true
terminal_packetization_forbidden_for_this_proof=true
preterminal_exact_uv_fiber_absolute_mass_dispersion_proved=false
new_actual_source_entropy_theorem_proved=false
row_column_unconditional_closed=false
```

## 1. 压缩

压缩前：

```text
NonterminalExactUVFiberAperiodicityEstimateForActualPreCauchySource
```

压缩后：

```text
PreTerminalExactUVFiberAbsoluteMassDispersionTheorem
```

绝对质量分散合同：

```text
For the actual pre-Cauchy source measure before any terminal extraction, disintegrated over exact (u,v) fibers in one formal unit, prove an absolute mass cap M_{u,v}^{abs} <= M^{abs}/L^K or the equivalent absolute L2 fiber energy bound. The proof may not use terminal packet return, canonical branch import, or signed-only cancellation.
```

## 2. 判定表

| gate | closed | proved | meaning | remaining |
| --- | --- | --- | --- | --- |
| `NonterminalFiberAperiodicityTargetActive` | `true` | `false` | 上一层已把 pre-terminal 支撑/容量定理压成 exact (u,v) fiber 非集中估计。 | NonterminalExactUVFiberAperiodicityEstimateForActualPreCauchySource |
| `SeedOnlyModelBlocksFormalProof` | `true` | `true` | 抽象 seed 字段本身允许全部质量集中到单个 exact pair，不能推出 fiber 非集中。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `SignedCancellationNotEnoughForAbsoluteCapacity` | `true` | `true` | source capacity 使用 fiber 绝对质量；只证明带符号和有相消，不足以排除同一 fiber 内绝对质量集中。 | absolute fiber mass dispersion, not only signed cancellation。 |
| `SourceObjectCannotBeRecoveredFromZeroRowGeometry` | `true` | `true` | 早期零行覆盖、斜线/圆柱/轮筛几何和 downstream payment 图不能反推 pre-Cauchy source 对象。 | source object must be declared upstream。 |
| `ElementarySupportLemmaAlreadyImported` | `true` | `true` | 若 absolute fiber mass dispersion 成立，支撑下界和 source entropy 已由初等链条给出。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `ExternalKLSWouldBeDifferentLane` | `true` | `true` | 外部 Full-S KLS/dispersion 可作条件线，但不是 strict 自足 nonterminal fiber 证明。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `TerminalPacketizationForbiddenHere` | `true` | `true` | 把大 fiber 打包后再送 PDEC/CleanKLS 会回到固定点；当前目标必须是源侧直接估计。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `AbsoluteFiberDispersionAtomPinned` | `true` | `false` | 最新最窄数学原子是 actual pre-Cauchy source 在 exact (u,v) fiber 上的绝对质量分散定理。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `PreTerminalExactUVFiberAbsoluteMassDispersionCurrentCorpusProved` | `false` | `false` | 当前语料尚未证明该绝对 fiber 质量分散定理。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |

## 3. 结构结论

当前不是证明 signed exponential cancellation，而是证明 absolute source mass 不集中在单个 exact fiber。CRT/轮筛刚性和早期零行覆盖只约束位置；source identity 只给对象；相消只给带符号和。要推出 source entropy，必须直接证明 pre-terminal exact fiber 的绝对质量分散。

## 4. 下一主攻点

```text
PreTerminalExactUVFiberAbsoluteMassDispersionTheorem
```
