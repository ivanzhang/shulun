# Prime Matrix strict trace 回流后当前全局前沿同步路由器

**状态：** `current_global_frontier_after_trace_cycle_reduced_to_terminal_three_atoms_open`

本步把最新三条链同步到同一个全局前沿：branch trace 线已证明在当前内部材料中回到 signed payload/row-level 表；终端叶子当前实例已把 PDEC/sparse 未来 schema 移出活动硬点；noncanonical 合法闭合模式已压成 actual-source bridge，而 actual-source 熵的 ExactUV/pair-mass 内部脊柱又被固定点防火墙识别为递归。因此，若不新增真正 primitive trace 输入，也不接受外部 FullS-KLS-ext，strict 自足线当前只剩三个非循环原子：

```text
AcyclicTerminalCanonicalLockToCanonicalSourceBoundary
OR A1CleanBranchCanonicalSourceAdmission
OR ActualNoncanonicalCleanCoreMovingAtomExclusion
```

这不是命题转换，而是把所有已识别的自回流路线从活动证明路径中删掉后得到的最小剩余。

```text
same_theorem_target_preserved=true
no_theorem_switch=true
branch_trace_self_proof_eliminated=true
signed_source_fixed_point_eliminated_as_proof=true
terminal_leaf_current_instance_reduced=true
actual_source_pairmass_spine_rejected_as_recursive=true
strict_current_frontier_three_atoms=true
direct_unconditional_contradiction_found=false
row_column_unconditional_closed=false
```

## 1. 已删掉的伪出口

| route | why removed from active proof path |
| --- | --- |
| signed slot/value-map/source identity | 回到 row-level clean-core 原始生成表 |
| branch trace internal expansion | visible coordinate 可走，signed payload 回到 row-level 表 |
| raw direct PDEC/CleanKLS | 终端回流防火墙显示会回到 canonical-lock 或 actual-source 目标 |
| FutureExplicitPDEC/Sparse schema | 当前无物化实例，只是未来准入纪律 |
| generic WFD anti-atom | moving-delta 模型已反证，不能作为自足模板 |
| canonical RIW/Buchstab import | 只闭合 scoped canonical 分支，不能静默导入 noncanonical 补集 |
| ExactUV/pair-mass entropy spine | 在当前材料中形成 `T -> ... -> T` 固定点 |

## 2. 当前三原子

| atom | proof obligation | blocked shortcut |
| --- | --- | --- |
| `AcyclicTerminalCanonicalLockToCanonicalSourceBoundary` | 证明 acyclic terminal 证书同集推前进入 canonical source，且无 noncanonical payload 残留 | 不能由 canonical 分支已经闭合自动推出 |
| `A1CleanBranchCanonicalSourceAdmission` | 在 Cauchy/dispersion 前证明 clean A1 反例分支实际选择 canonical RIW/Buchstab 决策树源 | 不能从早期零行几何、payment 图或 canonical scoped 分支反推 |
| `ActualNoncanonicalCleanCoreMovingAtomExclusion` | 直接排斥通过所有回流测试后的 actual noncanonical clean-core moving same-`(u,v)` 大原子 | 不能由 formal WFD、Type/Fourier、K4/K6 或支撑能量形式引理免费推出 |

## 3. 判定表

| gate | closed | proved | meaning | remaining |
| --- | --- | --- | --- | --- |
| `BranchTraceSelfProofEliminated` | `true` | `true` | branch trace 格式正确，但当前内部展开的 signed payload 回到 row-level 表。 | 新 primitive trace 输入或终端三原子 |
| `SignedSourceFixedPointEliminatedAsProof` | `true` | `true` | signed source/assignment/origin 环不能自证 signed coefficient。 | 非循环源输入或终端下降 |
| `TerminalLeafCurrentInstanceReduced` | `true` | `true` | 当前物化 PDEC/sparse 叶子为零，future schema 是准入纪律。 | canonical-lock 或 noncanonical legal mode |
| `NoncanonicalLegalModeFiltered` | `true` | `true` | generic WFD 与 canonical 偷导均被过滤，noncanonical 合法模式压到 actual-source bridge。 | source admission 或 moving atom exclusion |
| `ActualSourcePairMassSpineRejectedAsRecursive` | `true` | `true` | ExactUV/pair-mass/rate-bearing/terminal/canonical 脊柱在当前材料中回到原 source entropy 目标。 | independent nonterminal source proof or direct moving atom exclusion |
| `ExternalFullSKLSSeparated` | `true` | `true` | FullS-KLS-ext 可作为外部条件合同，但不计为 strict 自足证明。 | explicit acceptance if external lane chosen |
| `CurrentStrictFrontierThreeAtomsPinned` | `true` | `false` | 所有当前自回流伪出口删除后，strict 自足线剩三原子。 | canonical-lock / source admission / moving atom exclusion |
| `AcyclicTerminalCanonicalLockCurrentCorpusProved` | `false` | `false` | 当前未证明 terminal 同集推前和无 noncanonical payload 残留。 | AcyclicTerminalCanonicalLockToCanonicalSourceBoundary |
| `A1CanonicalSourceAdmissionCurrentCorpusProved` | `false` | `false` | 当前未证明 clean A1 分支 Cauchy 前已选择 canonical 决策树源。 | A1CleanBranchCanonicalSourceAdmission |
| `MovingAtomExclusionCurrentCorpusProved` | `false` | `false` | 当前未证明 actual noncanonical clean-core moving same-`(u,v)` 大原子排斥。 | ActualNoncanonicalCleanCoreMovingAtomExclusion |
| `RowColumnUnconditionalClosureCurrentCorpusProved` | `false` | `false` | 三原子均未闭合，且 DStructure/Rankin 仍需独立验收。 | three atoms + DStructure/Rankin |

## 4. 下一最窄选择

当前最值得继续主攻的是：

```text
ActualNoncanonicalCleanCoreMovingAtomExclusion
```

理由：canonical-lock 和 A1 canonical admission 都要求把 noncanonical payload 消掉或转入 canonical source，现有链条没有新的 source identity 输入；moving atom exclusion 则直接对早期零行反例链诱发的 actual noncanonical clean-core 容量大原子开刀，更贴近“反例链 vs 真实结构链”的终端矛盾目标。

它的下一步不能再走 ExactUV/pair-mass 递归脊柱，而应尝试给出一个独立的、非终端回流的 moving atom 排斥机制：

```text
IndependentNonterminalMovingAtomExclusionForActualNoncanonicalCleanCore
```

若无法给出该机制，则只能接受外部合同或等待 DStructure/Rankin 独立验收，不能声明行/列命题无条件闭合。

## 5. 结论

当前最新全局前沿已经从 signed coefficient / trace / source entropy 的循环词表中清理出来。严格自足线真正剩余是三个终端原子，其中最贴近反例矛盾场的是 `ActualNoncanonicalCleanCoreMovingAtomExclusion`。本步没有证明它，只是把它从一堆自回流路线中剥离出来，作为下一轮实际可攻的独立目标。
