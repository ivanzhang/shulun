# 合著论著总依赖图

## 1. Prime Matrix 依赖链

```text
Row/Column counterexample
  -> small-factor lock / rough candidate split
  -> tail-anchor peeling (Tail-log4)
  -> double-rough主体锚容量场
  -> Structured-EHPD bad configuration
  -> D-structure exclusion theorem
  -> finite verification below threshold
  -> row/column theorem
```

当前状态：前半归约已经附录化；D-structure exclusion 与常数/外部输入仍需逐行顶刊级复核。

## 2. RH 依赖链

```text
Off-critical zero beta>1/2
  -> smooth prime anomaly (PC1)
  -> CRT rough-composite baseline transfer (PC2)
  -> sparse/dense decomposition (C3/C4/C5)
  -> internal no-cycle and tail closure (C6/C9)
  -> AEX and GEE global lower/upper contradiction
  -> no free escape channel
```

当前状态：U1--U5 作者侧审稿义务已完成；仍保留 external page/copyediting 与 independent referee verification。

## 3. 共同矛盾场骨架

```text
Hypothetical counterexample
  -> structured field of explanations
  -> finite named exits
  -> baseline subtraction
  -> capacity lower bound
  -> exit upper bound / descent / absorption
  -> contradiction or explicit remaining obligation
```

该骨架是合著论著的统一主题。

## 4. 闭合证书接口

每条链必须提供四类证书：入口、分解、出口、全局矛盾。当前合著稿中：

- Prime Matrix 已有入口/分解证书，出口排斥集中在 D-structure 与 Tail-log4。
- RH 已有入口/分解/出口账本与全局 GEE 合成，最终接受依赖独立审稿。
- 合著稿本身已形成依赖闭合证书，但不是两个最终无条件定理的替代品。
