# Prime Matrix strict alpha anchor-collar 过载命名回流路由器

**状态：** `strict_alpha_anchor_collar_overload_return_schema_closed_terminal_gap_open`

`AlphaFormulaAnchorCollarOverloadNamedReturnLedger` 已作为命名回流 schema 闭合：短纤维饱和、端点相位缺陷、孤立 sparse escape 与固定位移复用都不能形成 anchor 专属第四出口，只能进入 PDEC/SAE/ColumnCRT/LocalSurvivor/CleanKLS 的已登记终端门。但终端门本身仍未排斥；下一主攻点回到 `PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve`，并保留 `ExplicitModelGapAndFiniteDPRCLedger` 与 DStructure/Rankin 验收。

```text
same_theorem_target_preserved=true
no_theorem_switch=true
counterexample_assumption_only=true
alpha_formula_anchor_collar_overload_return_router_closed=true
alpha_formula_anchor_collar_overload_named_return_ledger_closed=true
alpha_formula_anchor_collar_overload_named_return_exclusion_proved=false
alpha_row_anchor_phase_emission_formula_proved=false
direct_unconditional_contradiction_found=false
row_column_unconditional_closed=false
```

## 1. 过载出口

| case | route |
| --- | --- |
| persistent short-fiber saturation | AnchorFiberSaturationPDECOrSAEReturn -> PDEC/SAE named schema |
| endpoint phase defect | AnchorCollarEndpointDefectPDECExclusion -> low-mod finite phase OR tail-core/fiber saturation |
| isolated sparse fiber escape | LocalSurvivor/SAE packet -> GlobalPDECorSparseTerminalExclusion |
| fixed column displacement reuse | ColumnCRT / refined PDEC -> GlobalPDECorSparseTerminalExclusion |
| diffuse residual after no finite signature | internal CleanKLS/DLS admission -> PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve |

## 2. 回流律

Anchor-collar overload is a named-return schema, not a new source of signed alpha coefficients. Persistent short-fiber saturation, endpoint phase defects, sparse escapes and fixed column reuse all have registered PDEC/SAE/ColumnCRT/LocalSurvivor routes; after the global terminal split their exclusion is exactly the PDEC-CAP or internal CleanKLS/DLS terminal problem, plus the explicit model-gap and DStructure gates.

## 3. 判定表

| gate | closed | proved | meaning | remaining |
| --- | --- | --- | --- | --- |
| `CounterexampleBranchGuardPreserved` | `true` | `true` | 本步仍只处理早期零行反例链强制出的 anchor-collar 过载，不用真实样本缺席替代排斥。 | row_column_unconditional_closed=false。 |
| `AnchorCollarOverloadTargetActive` | `true` | `false` | carry-shell 同余 row 公式闭合后，下一最窄点是短纤维满载或公式过载的命名回流纪律。 | AlphaFormulaAnchorCollarOverloadNamedReturnLedger |
| `CanonicalAnchorCollarImported` | `true` | `true` | 最小高素 anchor 已被限制到 x<q<sqrt((x+1)P)，固定 q 后 cofactor 位于短素数纤维。 | 过载必须发生在有限 formal-unit fiber 上。 |
| `SurvivorIdentityImported` | `true` | `true` | R_x 被无损分解为 prime survivors 与 canonical semiprime fibers；纤维覆盖失败等价于素数幸存为零或命名缺陷。 | PrimeSurvivorLowerBoundOrPDECSAEColumnReturn |
| `EndpointLowTailDichotomyImported` | `true` | `true` | 若过载表现为端点强负缺陷，则已拆成低模有限 CRT 相位坏集与高模 tail-core/fiber saturation。 | AnchorCollarEndpointDefectPDECExclusion |
| `FiberSaturationNamedReturnSchemaImported` | `true` | `true` | 固定 q 的短纤维近饱和没有 anchor 专属第四出口：持久进 PDEC，孤立进 LocalSurvivor/SAE。 | AnchorFiberSaturationPDECOrSAEReturn |
| `GlobalPDECorSparseSplitImported` | `true` | `true` | PDEC/SAE/ColumnCRT/LocalSurvivor 终端已调和为全局 PDEC-CAP 或内部 CleanKLS/DLS 大筛门。 | PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve |
| `CurrentMaterializedTerminalFrontierImported` | `true` | `true` | 当前已物化终端前沿清零；未来过载必须提交显式同 formal unit schema，而不是无名出口。 | future explicit schema discipline。 |
| `NoAnchorSpecificOverloadExit` | `true` | `true` | anchor-collar 过载不能作为独立剩余停留；它只会生成 PDEC/SAE/ColumnCRT/LocalSurvivor/CleanKLS 命名终端。 | PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve AND ExplicitModelGapAndFiniteDPRCLedger |
| `AnchorCollarOverloadNamedReturnLedgerClosedAsSchema` | `true` | `true` | 目标 ledger 作为命名回流 schema 已闭合：过载有登记出口且没有未命名第四类。 | 终端排斥仍未完成。 |
| `TerminalExclusionStillOpen` | `true` | `false` | 本步不证明 PDEC-CAP、内部 CleanKLS/DLS、模型余量或 DStructure/Rankin；它只把 anchor 过载送入这些门。 | PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve AND ExplicitModelGapAndFiniteDPRCLedger AND DStructureTailLog4FiniteRankinFullLedgerIndependentAcceptance |

## 4. 下一主攻点

```text
PDEC_CAP_OR_INTERNAL_CleanKLS_LargeSieve
```

并行保留：

```text
ExplicitModelGapAndFiniteDPRCLedger AND DStructureTailLog4FiniteRankinFullLedgerIndependentAcceptance
```
