# Prime Matrix strict 非循环 signed coefficient 发射核证书

## 1. 结论

本步继续硬攻非循环 signed coefficient 发射核。现有 Möbius/奇偶材料能帮助理解符号结构，但没有给出 actual noncanonical emitter 的 Cauchy 前声明、公式行、非零 local factor 和推前前恒等式。因此 kernel 的第一字段仍是 `PreCauchyConstructorDeclarationLineForActualNoncanonicalEmitter`；这保持同一目标命题，只是把 signed-source 固定点的入口首字段钉死。

## 2. 状态

- same_theorem_target_preserved=true
- mobius_truncation_rejected_as_exact_kernel=true
- source_table_pins_precauchy_declaration_first=true
- pre_cauchy_constructor_declaration_line_proved=false
- noncircular_signed_coefficient_emission_kernel_proved=false
- row_column_unconditional_closed=false

## 3. 判定表

| gate | closed | proved | meaning | remaining |
| --- | --- | --- | --- | --- |
| `NoncircularKernelTargetActive` | `true` | `false` | 上一层 signed-source 固定点切断后，真正单点变成非循环 pre-Cauchy signed coefficient 发射核。 | NoncircularPreCauchySignedCoefficientEmissionKernelForActualNoncanonicalPrimitiveRows |
| `MobiusTruncationIsDiagnosticNotExactKernel` | `true` | `true` | 截断 Möbius 短除数权只给粗数指标近似/相关性，不保证 exact signed row、local factor 或推前前恒等式。 | PreCauchyConstructorDeclarationLineForActualNoncanonicalEmitter |
| `ThreeEdgeParityAuditsAreLocalShadowsNotEmitter` | `true` | `true` | 三点/三边奇偶审计解释局部符号影子，但仍是有限审计，不声明 actual noncanonical emitter。 | PreCauchyConstructorDeclarationLineForActualNoncanonicalEmitter |
| `SourceTablePinsFirstKernelField` | `true` | `true` | 已有 actual emitter source table 证书把源表首字段固定为 pre-Cauchy constructor declaration line。 | PreCauchyConstructorDeclarationLineForActualNoncanonicalEmitter |
| `PreCauchyDeclarationLineStillOpen` | `true` | `false` | declaration line 已被分类到 actual constructor formula line，但未证明。 | ActualNoncanonicalPrimitiveConstructorFormulaLineForEmitter |
| `ActualConstructorFormulaLineStillOpen` | `true` | `false` | actual constructor formula line 仍缺显式 alpha/delta 规则、域准入、行输出和失败回流。 | ExplicitAlphaDeltaPrimitiveConstructorRuleForActualNoncanonicalEmitter |
| `ExplicitAlphaDeltaRuleStillOpen` | `true` | `false` | 显式 alpha/delta 规则仍缺两侧 primitive rule、配对兼容和非零 sign/local factor。 | ActualNoncanonicalSourceTupleToAlphaSidePrimitiveRuleLedger |
| `AlphaEmissionRouteHitsSignedLiftAgain` | `true` | `false` | alpha 侧行发射路线先到锚/相位公式，unsigned 骨架闭合后又回到 signed lift。 | signed coefficient layer。 |
| `UnsignedGeometryClosedButSignedLayerOpen` | `true` | `false` | carry-shell、P列锚、layered-wheel 和 anchor-collar 只关闭 unsigned skeleton；signed 值表仍开放。 | AlphaFormulaSignedCoefficientLiftLedger。 |
| `NoncircularKernelCurrentCorpusProved` | `false` | `false` | 当前材料没有给出不依赖下游恢复的 exact pre-Cauchy signed coefficient 发射核。 | NoncircularPreCauchySignedCoefficientEmissionKernelForActualNoncanonicalPrimitiveRows |

## 4. Declaration 首字段

| field | meaning |
| --- | --- |
| `declared_before_cauchy` | constructor/emitter 在 Cauchy、dispersion、Phi/payment 之前声明。 |
| `same_formal_unit` | source tuple、basis word、signed coefficient 和 return tag 使用同一 formal unit。 |
| `actual_noncanonical_scope` | 声明不能偷换为 canonical、generic WFD、external spectral 或 mixed/unregistered source。 |
| `formula_line_pointer` | 声明必须指向 actual noncanonical primitive constructor formula line。 |
| `no_downstream_choice` | 声明不得读取 payment skeleton、早期零行 cover、origin table 或终端证书。 |

## 5. 下一真正单点

PreCauchyConstructorDeclarationLineForActualNoncanonicalEmitter

