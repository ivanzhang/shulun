# Prime Matrix strict 绝对 fiber 质量分散原像层路由器

**状态：** `strict_absolute_fiber_mass_dispersion_reduced_to_primitive_emitter_multiplicity_open`

`PreTerminalExactUVFiberAbsoluteMassDispersionTheorem` 进一步压成 primitive emitter 的原像纤维分散：单个 summand 权重已被容量乘子纪律吸收，真正剩余是证明 exact `(u,v)` fiber 的原像 multiplicity 不能承载总绝对质量的多对数级大份额。该分散定理尚未证明，行/列命题仍未无条件闭合。

```text
same_theorem_target_preserved=true
no_theorem_switch=true
absolute_fiber_mass_dispersion_router_closed=true
known_summand_weight_cap_imported=true
reverse_payment_recovery_rejected=true
crt_wheel_fiber_map_not_expansion=true
preterminal_exact_uv_primitive_emitter_multiplicity_dispersion_proved=false
new_actual_source_entropy_theorem_proved=false
row_column_unconditional_closed=false
```

## 1. 压缩

压缩前：

```text
PreTerminalExactUVFiberAbsoluteMassDispersionTheorem
```

压缩后：

```text
PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem
```

multiplicity 分散合同：

```text
For the actual primitive pre-pushforward emitter, with each summand carrying branch key, sign/local factor and exact (u,v) map, prove that every exact (u,v) fiber receives at most a log-power-small fraction of the total emitted absolute weight, after the registered single-summand weight cap.
```

## 2. 判定表

| gate | closed | proved | meaning | remaining |
| --- | --- | --- | --- | --- |
| `AbsoluteFiberMassTargetActive` | `true` | `false` | 上一层已把非终端 fiber 非集中压成 exact fiber 绝对质量分散。 | PreTerminalExactUVFiberAbsoluteMassDispersionTheorem |
| `KnownCoefficientWeightCapImported` | `true` | `true` | 单个 summand 的 Type/Fourier/fiber/系数损失已登记为 log-power 成本；权重上界不是当前主障碍。 | 需要控制同一 exact fiber 接收多少 primitive summand。 |
| `EmitterOriginLedgerNeeded` | `true` | `false` | 绝对质量分散必须在 pre-pushforward primitive emitter 上计数；当前材料仍未证明原始生成账本存在。 | CleanCoreOriginalCoefficientGenerationLedgerAndReturn。 |
| `ReversePaymentFunctorInsufficient` | `true` | `true` | 从 payment 图反向恢复 preimage summand 不唯一；不能由下游图推出原像纤维分散。 | PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem |
| `SourceLoopCutImported` | `true` | `true` | origin ledger -> constructor -> formula -> emitter -> origin ledger 的循环不能生成新来源或分散估计。 | PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem |
| `MassDispersionReducedToMultiplicity` | `true` | `false` | 在 summand 权重已受 log-power 控制后，absolute fiber mass cap 的数值负担就是 exact `(u,v)` 原像纤维的 multiplicity spread。 | PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem |
| `CRTWheelGivesFiberMapNotExpansion` | `true` | `true` | CRT/轮筛/层叠筛能定义 exact `(u,v)` fiber map，却不证明该 map 的最大原像纤维小。 | PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem |
| `PrimitiveEmitterMultiplicityDispersionCurrentCorpusProved` | `false` | `false` | 当前语料尚未证明 primitive emitter 在 exact `(u,v)` 原像纤维上的多对数级分散。 | PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem |

## 3. 结构结论

绝对质量分散不是新的相消问题，而是 emitter 原像纤维扩张问题。已登记权重上界只能把质量问题转为 multiplicity 问题；下游 payment 图和 CRT 纤维图不能反向证明原像纤维分散。

## 4. 下一主攻点

```text
PreTerminalExactUVPrimitiveEmitterMultiplicityDispersionTheorem
```
