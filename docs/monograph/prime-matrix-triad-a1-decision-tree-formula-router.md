# Triad-A1 Exact RIW Decision Tree Formula 路由审计

**状态：** `decision_tree_formula_reduced_to_source_coefficient_identification`

ExactRIWDecisionTreeFormula 已被压成源头系数识别：RIW/Buchstab 递归本身可展开为完整决策树，互斥与非零随结构成立；剩余是证明 A1/KZ-E 实际使用的 lambda_c 就是该 canonical 决策树系数，并且路径数没有超出 K6/polylog 预算。

## 1. 源头系数识别律

The exact RIW/Buchstab decision-tree formula is an algebraic expansion of the recursive sieve construction. It closes no-cancellation once the actual coefficient source is identified with that canonical construction and the complete trace count fits the K6 polylog budget. Thus the remaining obstruction is not a new estimate: it is source identification. The A1/KZ-E coefficient must be shown to be the canonical RIW/Buchstab tree coefficient; otherwise this internal support route is unavailable and the block must return or use external DI/BFI.

```text
RIW/Buchstab recursion => finite complete decision-tree expansion;
complete traces => disjoint support and nonzero same-path coefficients;
if actual KZ-E lambda_c is this canonical tree coefficient:
  ExactRIWDecisionTreeFormula follows;
else:
  this internal support route is unavailable; return or use external DI/BFI.
```

## 2. 汇总

- `path_partition_input_status=finite_signature_no_cancellation_reduced_to_exact_decision_tree_or_clean_return`。
- `path_partition_input_next_target=ExactRIWDecisionTreeFormulaOrCleanReturn`。
- `recursive_formula_closed_algebraically=True`。
- `all_trace_budget_rows_suffice=True`。
- `conditional_source_identification_implies_decision_tree_formula=True`。
- `decision_tree_formula_closed=False`。
- `next_internal_target=ActualKZESourceCoefficientIdentificationOrCleanReturn`。
- `terminal_gap_after_router=ActualKZESourceCoefficientIdentificationOrCleanReturnOrExternalDIBFIOriginalDispersion`。

## 3. 门控表

| gate | available | needed | gap | route | closed |
| --- | --- | --- | --- | --- | --- |
| `RIWBuchstabRecursiveFormula` | RIW/Buchstab weights are generated by recursive inclusion-exclusion branches | write alpha/delta as a finite signed sum over complete branch traces | algebraic expansion must be recorded in the A1 ledger, but it is not a spectral estimate | expand the recursive sieve identity into complete traces | `True` |
| `TraceDisjointnessAndNonzero` | previous path router proved this once complete traces exist | complete traces are disjoint and same-trace coefficients are nonzero | none after the recursive formula is fixed | use deterministic branch history and nonzero local factors | `True` |
| `TraceCountWithinK6Budget` | K6/tail-label bookkeeping controls polylog branching | complete trace count remains within the signature budget used upstream | must be checked for the exact truncation depth and branch alphabet | record depth/options; over-budget trace explosion returns to K6/PDEC/SAE | `False` |
| `ActualKZESourceCoefficientIdentification` | current chain says exact RIW/Buchstab source is needed | the actual WFD coefficient entering A1/KZ-E equals this canonical recursive coefficient | formal well-factorable existence is weaker than source identification | tie KZ-E lambda_c to the canonical RIW/Buchstab construction, not to arbitrary WFD factors | `False` |
| `NonCanonicalSourceCleanReturn` | external DI/BFI and PDEC/SAE exits exist | if the source coefficient is not the canonical tree coefficient, clean A1 cannot use this support route | the clean-branch contract must state this explicitly | noncanonical source goes to external DI/BFI or returns as missing-row/PDEC/SAE | `False` |
| `DecisionTreeFormulaImpliesPathPartition` | recursive formula + source identification + budget | ExactRIWDecisionTreeFormulaOrCleanReturn | conditional implication is direct; source identification remains | combine recursive expansion with KZ-E source identification | `True` |

## 4. 路径数预算模型

| k | log y | trace count | polylog budget | suffices |
| ---: | ---: | ---: | ---: | --- |
| 3 | 6.90776 | 8 | 329.618 | `True` |
| 4 | 9.21034 | 8 | 781.317 | `True` |
| 5 | 11.5129 | 8 | 1526.01 | `True` |
| 6 | 13.8155 | 8 | 2636.94 | `True` |
| 7 | 16.1181 | 8 | 4187.37 | `True` |
| 8 | 18.4207 | 8 | 6250.53 | `True` |
| 9 | 20.7233 | 8 | 8899.68 | `True` |

## 5. 结论

新最窄内部目标为：

```text
ActualKZESourceCoefficientIdentificationOrCleanReturn:
  prove the A1/KZ-E lambda_c is exactly the canonical RIW/Buchstab decision-tree coefficient;
  prove the complete trace count fits the K6/polylog budget;
  otherwise route the block to PDEC/SAE or external DI/BFI.
```

这仍不是行命题最终闭合；但它把 exact 决策树公式本身降为源头系数识别问题。
