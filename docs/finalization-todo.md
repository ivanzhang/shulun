# 定稿清理 TODO

本文档用于把当前追加式研究稿整理成一致定稿。原则：保留数学诚实，不把脚本化阈值 `log_P_scripted≈84.25` 误写成最终全部奇素数验证阈值。

## 1. 必须统一的最终结论层级

- 当前可定稿主张：条件化高段证明框架；不能声称已经无条件证明充分大版本。
- 高段升级主张：若第 4.3 行侧短段接口、列侧 `LC`、B.0.4* 覆盖终审、A.7.0 D4K5 尾部尺度稳定和显式阈值账本全部闭合，则得到充分大奇素数版本。
- 全部奇素数升级主张：在高段升级主张基础上，若完整 `P_*` 抽取并完成 `P≤P_*` 有限验证或等价证书，则所有奇素数版本成立。
- 不应在摘要、引言或结论中写“已经证明所有奇素数成立”。

## 2. 旧状态段落清理

以下文件是追加式记录，含旧状态，需要最终定稿时保留最新状态或移入历史附录：

- `docs/final-proof-review.md`：已压缩为最新审稿摘要；完整历史归档到 `docs/archive/final-proof-review-full-history.md`。
- `docs/final-closure-obstacles.md`：已压缩为最新障碍摘要；完整历史归档到 `docs/archive/final-closure-obstacles-full-history.md`。
- `docs/explicit-constant-audit.md`：已新增当前可赋值常数表，并将旧阻塞描述移入历史记录区。

## 3. 当前脚本化账本状态

运行：

```bash
python3 experiments/explicit_threshold_ledger.py --show-template > /tmp/constants_current.json
python3 experiments/explicit_threshold_ledger.py --constants /tmp/constants_current.json --cheb-target-safe --final-ledger --max-log-p 100000
```

当前输出：

- `log_P_scripted≈84.25449700383089`（脚本压缩值，使用 `delta_row=1/4`；安全间隙复核后可能回退到约 `92.36`）。
- 该值涵盖 tail、tiny、low-moment、row/col error、B.0.4* parameter gates；当前脚本参数为 `A1=8,S0=12,C_low=4,A_row=4,delta_row=1/4`，其中 `delta_row` 仍需安全间隙复核。
- 该值不涵盖最终有限验证可执行性。

## 4. 仍需处理的全局问题

- 行侧第 4.3 接口：短段双线性交点上界/粗半素数填充链仍需证明，不能由局部几何直觉替代。
- 列侧 `LC`：需要点态高阈值容量下界，或可覆盖全部非零剩余类的有限/区间证书。
- D4K5 尾部：`q=0.95,s=0.2` 递推极限 `4<5`，最小余量 `0.08870962029806143`；攻坚档 `q=0.958` 递推极限 `4.7619<5`，最小余量 `0.10226434483825808`。仍需证明块化正峰包络的无限尾部递推不变量，或给出替代显式尾部定理。
- R5-tail 误差预算：`q=0.958` 下可承受 `0.25X^{-1/4}\log X` 级 dyadic 延拓误差；若只能得到 `X^{-1/4}\log^2X`，常数需 `<0.0177`，不宜作为首选路线。
- R5-tail 最新诊断：已有连续证书和远端样本在 `q=0.958` 下实际 `K_needed=0`；首选证明目标改为“初始段全局最坏”或“远端余量不回落”引理。
- R5-tail 机制拆分：证明低块 `a≤400` 峰值衰减，同时证明中高块总容量受控；避免再尝试整个比例核心单调。
- R5 低块目标：优先证明 `B_{≤400}(X)≤0.82X^{-1/4}logX`；细化证书已定位最坏窗口和责任块，粗绝对值路线已排除，当前最优硬点是固定偏移四层分解：普通重团 `W_h≥60,N_h>25` 时平均核 `≤5e-4`，短链 `N_h≤20`、过渡 `21≤N_h≤25`、边界/轻团用平方能量或有限表吸收；全偏移分层证书已支持该拆分，下一步需证明异常层 `L2≤0.16` 能量界，重点证明过渡层两个子目标：高尾段型已因子化为平均核 `≤3.7e-4` 与权重平方和 `≤366`，低起点型已压到单团贡献 `≤0.12` 的有限模板目标，短链/轻边界项已形成样本目标 `E_{S,L}≤0.13`，实际场账本显示 `ordinary_contrib+exceptional_L2≈0.2898<0.357`，下一步严证异常 `L2` 到总贡献的 Cauchy/正交接口，目标为危险正峰区有效支撑 `R_eff≤36`，并将其严写为总异常层有效支撑的三变量“不共峰”有限表引理；`eta` 分割仅作为大团枚举辅助，并接固定块端点包络/dyadic 继承；尾块容量另行处理。
- [ ] R5：补全异常层互斥分块能量场证明，目标是从 `(W_h,N_h,K_h)` 四类有限表推出 `R_eff<=36` 或危险区等价替代界。
- `P_M`：Mertens 乘积显式阈值的正式引用和数值。
- `P_G`：行安全间隙所有 `o(1)` 项的独立阈值。
- `P_cap`：列容量余量所有 `o(1)` 项的独立阈值。
- `B.0.4*`：覆盖矩阵与 B.0.3e+ 的最终人工审稿确认。
- 有限验证：`exp(84.25)` 不可直接枚举，需继续降阈值或发展不可枚举区间的验证替代策略。

## 5. 最近已闭合的局部硬点

- Dusart theta 界给 `C_Cheb=1+1/36260`。
- 低阶矩可取 `C_low=4`，闸门 `C_d4=10^4,C_4loc=10^8`；继续压到 `C_low=3` 会被 `d4` 闸门卡住。
- 行列误差闸门已脚本化，模板 `C_err=10^12,A_err=8`。
- B.0.4* 参数账本给 `C_*=160`。
- 坏层覆盖闭包 2.0 已补。
- `b(a)` admissibility 已由 B.0.3e+ 补齐。
- [ ] R5：把分块能量证书升级为四类 `(W,N,K)` 有限模板线性-二次规划，证明反共峰曲线 `L<=Psi(R)`。
- [ ] R5：实现 `W/N/K` 模板整数二次规划证书，禁止仅用块数和单块上界的过松 water-filling。
- [ ] R5：证明四层资源曲线 `L_light<=0.26` 与 `L>=0.35 => E^2>=L^2/20`，替代 804 模板块级 IQP。
- [ ] R5：证明 light 层短偏移--长尾互补容量曲线，目标 `max_u u+Theta_75(u)<=0.26`。
- [ ] R5：将 `H=80` light 层三条有限模板不等式严格化：低前缀总量、高前缀上界、高前缀长尾上界。
- [ ] R5：构造高前缀锁定相位单元表，证明 `U>=0.16` 时 `W_tail<=502,N_tail<=75,V<=0.074`。
- [ ] R5：证明聚合资源锁 `U80>=0.1457,W_pref>=510` 导出有限相位单元，并逐单元验证 `V80<=0.074`。
- [ ] R5：把 4 个资源锁相位单元的核心 `h/a` 骨架表转写为可审查有限验证表，闭合 `V80<=0.074`。
- [ ] R5：把资源锁覆盖四单元的相位模板枚举严格化；逐行 `V80` 有限表已形成 compact 证书。
- [ ] R5：将资源锁互补证书转成相位模板证明：区间级 `max U`/`max W` 表 + 12 个边界点表。
- [ ] R5：将 light 主证书的 6 个断言转写为正式有限模板定理，区分窗口证书与全局证明。
- [ ] R5：将高质量能量证书转写为层资源曲线证明，重点闭合 tail_absent/short+light 最坏支路。
- [ ] R5：构造 tail-absent short/light 联合前沿证书 `Qmax(S), A(S), B(Q)`，闭合二维势能谷。
- [ ] R5：将二维前沿分箱证书转写为模板不等式，尤其闭合 `S∈[0.14,0.145)` 的最小余量箱。
- [ ] R5：对 4 个 pure short/light 谷底点构造 short 资源锁与 light 补偿锁，逐点验证势能正性。
- [ ] R5：将四点谷底 short 资源锁转写为模板枚举证明，闭合 `S∈[0.14,0.145)` 最小箱。
- [ ] R5：将四点谷底固定目标簇 `{1088388,1088400,1088472,1088544,1088640}` 转写为相位锁模板证明。
- [ ] R5：把五目标模板交叠带 `[1088370,1088375]` 与两点质量不足排除写成正式有限表。
- [ ] R5：按 master map 逐一模板化 4 个剩余接口：light H80、high-mass 层曲线、tail-absent 前沿、四点固定目标簇。
- [ ] R5：按四点固定目标簇样板，继续模板化 light H80、high-mass 层曲线和 tail-absent 二维前沿。
- [ ] R5：审查三张 template-proof JSON，将每步标记为“可全局模板化”或“需作为有限验证证书提交”。
- [ ] R5：优先证明或证书化 (A.7.0-R5global1)--(A.7.0-R5global4)，否则不能宣称全局无条件闭合。
- [ ] R5：专攻 A.7.0-R5phasecell-extremum，建立相位单元分解、导数临界点表与候选点有限枚举。
- [ ] R5：为 phasecell-extremum 建立导数根隔离证书格式，覆盖 x^{-3/4}、log x 与平方和泛函。
