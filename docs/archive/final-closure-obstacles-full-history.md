# 最终闭合障碍清单

> **最新状态以本节为准。** 下方段落保留历史攻坚记录；其中旧阻塞项若与本节冲突，以本节为准。

## 0. 最新剩余障碍总表

| 类别 | 当前状态 | 下一步 |
|---|---|---|
| 结构性证明缺口 | 已压缩为有限表格审稿 | 最终人工核对 6.14.2c 与 B.0.3e+ |
| 常数脚本化 | 已有 `--final-ledger` | 补 `P_M,P_G,P_cap` 具体阈值 |
| 当前脚本化阈值 | `log_P_scripted≈484.03986414399714` | 继续降阈值或换验证策略 |
| 有限验证 | `P≤1000` 通过 | 无法直接覆盖 `exp(484)` |
| 定稿表述 | 充分大版本可作为当前主线 | 全部版本只能作条件升级 |

## 0.1 最小剩余全局障碍

当前不再优先寻找新的局部坏层；最小剩余全局障碍是：把脚本化阈值继续降到可验证范围，或发展一个能覆盖 `P≤P_*` 的非枚举有限验证/区间证明策略。


---

## 历史记录区

以下内容为研究过程中按时间追加的复核/障碍记录；若与顶部最新总表冲突，以顶部最新总表为准。

日期：2026-04-28

## 1. 当前主线状态

本文当前主线采用硬窗口路线：

\[
USC/DBA+四点能量+离散coarea+DBA\text{-}closure
\Longrightarrow B.0.4^*
\Longrightarrow 行命题,
\]

列命题仍走高阈值列场 USC、一致集中、容量比较与坏列整数化。平滑 `B.0.4S/B.0.4S-short` 只保留为备选路线，不参与主线闭合。

## 2. 最后硬障碍

### 2.1 `B.0.4*` admissibility 核查

需要逐项确认附录 B 叶剥离产生的所有对象满足：

- `Q\le \log^{C_Q}P`；
- `b(a)` 在正常层是有限复杂度有理/CRT 可逆分片函数；
- `W(a)` 有统一有界变差或可分片为有界变差权；
- 非可逆分母、导数小、ramification、Jacobian 退化与步长共振均进入 `6.14` 六类坏层。

若某一项不满足 admissibility 且不落入坏层，则它是新的唯一剩余缺口。

### 2.2 `USC(log P)` 常数增长

需要把附录 A 的 connected cumulant 展开整理为正式引理，重点核查：

- `r≤c\log P` 下 Bell/Möbius 分区常数不爆炸；
- skeleton 计数与 tiny/near/far 三层 Rankin 账本可和；
- 中心化叶剥离在高模数 CRT 投影中只产生可吸收误差。

### 2.3 四点能量到 UAS 的逆推

需要确认四点能量异常确实反推短弧少根性失败，且所有退化四元组已经被：

- 对角/半对角计数；
- `4E-DISP`；
- 离散 coarea；
- `DBA-closure` 坏层矩阵

完整覆盖。

### 2.4 列方向坏列整数化

列命题的最终闭合需要把坏列期望/概率界严格转成坏列数 `<1`，并核对容量比较中 `e^{-γ}>2(1-α)` 的余量没有被对数损失吃掉。

### 2.5 显式阈值与有限验证

当前扩展数值验证已覆盖 `P≤10000`，但仍只是 `P_{max}` 范围验证。若论文要声明“所有奇素数”，必须先从所有渐近估计中抽取显式 `P_*`，再运行：

```bash
python3 experiments/verify_finite_p_grid.py --max-p P_* --quiet
```

## 3. 未处理文件决策

### 3.1 应纳入证明稿的文件

- `docs/final-proof-draft.md`
- `docs/final-proof-review.md`
- `docs/final-closure-obstacles.md`

这些文件共同描述当前主线、审查结论与最后硬障碍。

### 3.2 暂不纳入主证明提交的文件

- `docs/rigid-patch-lemma-experiments.md`
- `experiments/rigid_patch_lemma_scan.py`
- 当前 134 个未跟踪 `experiments/*.py`

原因：这些文件多为探索性数值实验或长日志，其中 `docs/rigid-patch-lemma-experiments.md` 已达约 1.9 万行，更适合作为后续实验资料分批整理，不宜混入最终证明稿提交。

## 4. 下一步严写顺序

1. 以 `6.17` 为索引，逐项反查附录 B 中每个边界项的 `W(a),b(a),Q` 来源；
2. 对每个非 admissible 分片标注其进入 `6.14` 哪一类坏层；
3. 把 `USC(log P)` 的常数增长账本压缩成 5--7 个正式命题；
4. 对列方向补一张“坏列整数化与容量余量表”；
5. 最后抽取显式 `P_*` 并做有限验证。

## 5. 数学诚实声明

当前文稿已经把主线最终缺口压缩到有限清单审查：`B.0.4*` admissibility、`USC(log P)` 常数增长、四点能量退化覆盖、列坏列整数化与显式阈值。只有这些项目全部严写通过后，才能声明无条件完整证明闭合。


## 6. 本轮新推进：admissibility 与相消分层

已在 `docs/final-proof-draft.md` 的 `6.17.6--6.17.7` 增补核查：附录 B 生成的 `Q,b(a),W(a),N,Y` 均能映射到 `B.0.4*` 的 admissible sawtooth 接口；失败情形进入小筛剔除、低体积盒或 `DBA-closure` 坏层。

但该核查只说明接口无遗漏，不等于自动证明 sawtooth 相消。真正硬解析核心仍是 6.7--6.15 的 UAS/FNL、四点能量、离散 coarea 与 DBA-closure 链条。后续若要无条件闭合，应继续严写该链条，而不是再扩大实验或平滑素数权路线。


## 6.18 sawtooth 相消链条严写补充

新增 `6.18` 节把硬窗口 `B.0.4*` 的 sawtooth 相消拆成三段：`LV` 大 Fourier 值反推短弧集中，`AE` 短弧集中反推四点厚化能量异常，最后由四点 rank、离散 coarea 与 `DBA-closure` 排除异常。该节还把旧的 `FNL/NL => B.0.4S-short` 主线修正为 `FNL/NL => B.0.4*`，平滑素数权接口继续只作为备选路线。

当前最后审稿点进一步缩小为：复核 6.14.2a 的 resultant atlas 是否覆盖所有 coarea/Jacobian/ramification 坏层。参数表已纳入 LV/AE 新增损失。


## 6.18.2 AE 层化能量严写补充

本轮专攻 `AE`：把原先粗略的 `M^4/L` 四点能量下界修正为安全的层化 Holder 下界 `M^4/L^3`。新增短弧层化定义、层化四点能量引理和严写版 `AE` 命题。结论是：若 `L=O(1)` 或 `L\le\log^{O(1)}P`，该损失可由参数余量吸收；若某分片导致 `L` 超过对数幂，则它不再是组合能量问题，而必须作为分母、导数或 ramification 坏层进入 `DBA-closure`。

这次修正提高了逻辑严谨性：短弧集中并不无条件给出过强能量下界，而是经层化后给出可审查的厚化四点异常。


## 6.18.1 LV 加权 van der Corput 严写补充

本轮专攻 `LV`：将原先概略的“大 Fourier 值反推短弧集中”拆成三个标准引理：加权 van der Corput、大值给出大相关步长、相关大值反推短弧集中。新增了权重总变差 `Var(W)` 的显式账本，说明 Stieltjes 卷积权在 dyadic 正常盒内只带来对数幂损失，由 6.15 参数余量吸收。共振步长 `q|h`、`q|mT` 与分母不可逆层继续交由 6.14 的步长共振层和分母层处理。

这使 `LV` 从直觉性反推变成可审查链条；本轮后 `W_h` 的有界变差继承与共振步长剔除已被写成独立微引理，剩余只需在最终审稿中核对其参数常数。


## 6.18.1d--e 权重继承与非共振密度补充

本轮继续排除 `LV` 的两个最小硬点：新增 `W_h` 变差继承引理和非共振步长密度引理。`W_h(a)=W(a+h)W(a)` 的变差由乘积变差不等式控制，保持对数幂损失；坏步长由 `q|h`、`q|mT` 和分母不可逆因子生成，在步长平均中贡献 `O(1/q+1/H)`，由 6.14 的 DBA-closure 与 6.15 的 `B_4` 余量吸收。

因此 `LV` 的剩余问题不再是结构性缺口，而是最终参数常数复核。


## 6.18.2b AE 层数二分补充

本轮专攻 `AE` 的层数 `L`：新增“层数二分”引理，说明正常 dyadic 盒中 `F_t=N_t/D_t` 的短弧可分辨层数满足 `L≤log^{C_L}P`；若层数失控，则必然来自分母极点、小导数或 ramification/resultant 分支合并，分别进入 6.14 的分母层、导数层和 ramification 层。因此 `L^3` 损失在正常层中只是对数幂，可由 6.15 参数余量吸收。

这一步把 `AE` 的层化损失从剩余硬点降为 DBA 覆盖矩阵的一项核查。


## 6.14--6.15 最终覆盖矩阵与参数表补充

本轮把 `6.18` 新增退化源纳入 `6.14.2` 覆盖矩阵：`W_h` 阶梯端点、非共振步长剔除、`L` 层数失控、层化四点厚化异常分别映射到 dyadic/低体积账本、步长共振/分母层、分母/导数/ramification 层、四点 rank/Jacobian/coarea 坏层。`6.15` 参数表同步加入 `C_LV` 与 `C_AE`，并把 `L^3`、`W_h` 变差、步长平均等损失纳入 `C_*`。

至此 `LV` 与 `AE` 不再作为独立结构缺口；最后核心审查点集中到 `6.13--6.14` 的 coarea、Jacobian、ramification、resultants 坏层覆盖是否真的无遗漏。


## 6.14.2a resultant atlas 补充

本轮新增 `6.14.2a resultant atlas`，把最后的 coarea、Jacobian、ramification、resultants 坏层写成有限生成清单：分母极点、一变量导数、四点投影 Jacobian、水平纤维判别式、分支合并 resultant、四点 rank 失效、Jacobian 公共分支、LV/AE 层化失控、步长/频率共振。

文稿现在给出无遗漏审查原则：若某坏情形不在 atlas 中，则它同时满足分母非零、导数非零、投影 Jacobian 非零、无 ramification、局部 rank 有效、步长非共振；在这些条件下 coarea、局部 rank 与 LV/AE 链条已经给出预算内估计。因此最后核心审查被压缩为复核 atlas 清单本身。


## 6.14.2b atlas 穷尽性补充

本轮新增 `6.14.2b atlas 穷尽性`。证明采用反设：若存在 atlas 外坏机制，则它必须同时满足分母非零、导数/Jacobian 非零、无 ramification、rank 有效、步长非共振；在这些正常条件下，`LV => AE => 四点 rank/coarea/参数余量` 已给预算内估计，矛盾。因此不存在 atlas 外“第七类退化”。

同时修正了 `6.14.3` 中重复的 resultant 高度公式。当前最后状态是：结构性坏层已压缩为 atlas 清单复核与有限生成高度账本复核。


## 6.14.3a--6.14.4a FGH 高度与坏素权重补充

本轮专攻有限生成高度账本：新增 `6.14.3a FGH 统一高度账本`，逐项列出 atlas 生成元的操作、次数与高度传播；新增 `6.14.4a 坏素权重闭合`，证明固定 skeleton 下所有坏层多项式的坏素因子权重仅为对数幂，并可并入 connected Rankin 账本和 `C_*` 参数余量。

这把“有限生成高度账本复核”从结构性缺口降为标准高度/resultant 与 Rankin 常数核查。


## 7. 最终审稿级复核状态

新增 `6.19` 复核表后，硬路线结构性缺口已压缩为有限审稿项目：atlas 无遗漏、FGH 高度账本、参数余量、列方向常数和显式阈值。当前严格表述仍应是“所有充分大奇素数”的证明框架；若要升级为“所有奇素数”，必须抽取显式 `P_*` 并运行完整有限验证。


## 8. 扩展有限验证状态

已运行 `P≤10000` 的有限验证，覆盖 1228 个奇素数，全部通过。但由于当前文稿尚未给出显式理论阈值 `P_*`，该验证仍不能替代“充分大之前全部验证”。下一步若要完成全体奇素数版本，必须先显式化所有渐近阈值并抽取 `P_*`。


## 显式阈值 P_* 抽取账本补充

本轮按审稿要求尝试抽取 `P_*`，结论是：当前文稿尚未数值化所有 `o(1)`、`O(1)`、`P^{o(1)}`、`\ll` 与“足够大”常数，不能诚实给出实际 `P_*`。已在 `docs/final-proof-draft.md` 新增 `7.2 显式阈值 P_* 抽取账本`，列出必须显式化的八类阈值：USC 高矩、行候选下界、行安全间隙、`B.0.4*`、参数余量、列候选主项、列容量余量、坏列整数化。

当前已完成 `P_{max}=10000` 的扩展验证，但这不能替代 `P≤P_*` 的最终有限验证。


## 7.2 常数数值化进展

本轮对 `P_*` 抽取账本做了第一步数值化：行安全间隙常数 `e^γ/4 log 3 = 0.4891770114`，加上 `θ_3=0.42` 得 `0.9091770114`，可保守取 `η=0.08`；列容量常数 `e^{-γ}=0.5614594836`，而 `2(1-0.85)=0.3`，绝对余量 `0.2614594836`，相对余量约 `0.4656782746`。

同时文稿新增阻塞表：`P_A,P_H,P_G,P_B,P_par,P_C,P_cap,P_int` 仍需显式化各自的 `o(1)`、`O(1)`、`P^{o(1)}`、`\ll` 常数。故本轮仍不能诚实给出实际 `P_*`。

## 7.3 高矩尾界显式参数化补充

本轮新增 `7.3 高矩尾界常数的显式参数化`。当前 `P_A,P_C,P_int` 的 Markov 尾界接口已改写为可计算参数账本：若附录 A 给出显式 `C_USC,c_0`，且 B/C 给出主量下界 `μ≥μ_0P^β/log^aP`，则可直接取 `κ=min(c_0/4,β/(8C_USC))` 与 `c_tail=κβ/4`，并检查有限个阈值不等式。

这一步排除了“尾界转换不显式”的小硬点；新的最小剩余硬点是从 connected cumulant、Bell 数、Rankin 尾部和容量主项中抽取实际数值 `C_USC,c_0,μ_0,a`。在这些输入常数完成前，仍不能诚实给出最终数值 `P_*`。

## 7.4 原子常数账本与阈值计算器

本轮新增 `7.4 原子常数抽取账本与有限验证闸门`，把 `C_USC,c_0` 拆为 `C_Bell,C_tree,C_edge,C_Rank,C_leaf,C_bd,C_Cheb,A1` 等原子常数，并给出 `C_USC=100(C_Euler+1)^2`、`c_0=1/(20C_Cheb A1)` 的安全合成公式。新增 `experiments/explicit_threshold_ledger.py` 作为机械阈值计算器：只有当所有原子常数均已赋值时，才会输出尾界阈值。

因此当前最后硬点进一步缩小为：逐项给原子常数赋数值，并把 B/C 的主量误差常数数值化。完成后才能运行 `verify_finite_p_grid.py --max-p P_* --quiet` 并把结论升级到全部奇素数。

## 显式常数赋值审计

本轮新增 `docs/explicit-constant-audit.md`，将常数赋值状态分为“可赋值外围参数”和“仍阻塞原子常数”。可安全使用的外围参数为 `C_Cheb=8,A1=100,delta_row=delta_col=0.1,d_row=2`；真正阻塞仍是 Bell/skeleton/局部兼容性/Rankin/叶剥离/tiny 边界与行列主量下界常数。故当前不能运行最终 `P≤P_*` 的有限验证。

## C_Bell 与 C_tree 显式赋值进展

本轮将两个组合常数从阻塞项移出：新增引理 2.5a 给 `C_Bell=16`，新增引理 2.6a 给有效 rank 区 `C_tree=64`。剩余最核心常数阻塞变为 `C_edge,C_Rank,C_leaf,C_bd,mu_row,a_row,mu_col,a_col`，以及 `C_tree` 与 `C_Rank` 在高 rank 尾部接口处的合并。

## C_edge 与 C_Rank 判据进展

本轮将 `C_edge` 从阻塞项移出：引理 2.6b 给出可取 `C_edge=128`。同时引理 2.6c 暴露了 `C_Rank` 的真实剩余硬点：高 rank 尾部需要一个显式 Rankin 权 `σ`，并验证 `σΞ_r≤1/2`。在该 `σ` 未从 A.4 构造中抽取前，`C_Rank` 仍不能赋值，最终 `P_*` 仍不能计算。

## C_leaf 与 C_bd 进展

本轮将 `C_leaf` 从阻塞项移出：引理 2.8a 给出可取 `C_leaf=8`。tiny 边界项也已局部显式化，倍数可取 `C_bd=4`；但它的最终吸收阈值依赖行/列主量下界，因此仍与 `mu_row,a_row,mu_col,a_col` 绑定。剩余核心硬点现在压缩为 `C_Rank` 与行/列主量常数。

## 行列主量常数接口进展

本轮把 `mu_row,a_row,mu_col,a_col` 从未知常数推进为条件可赋值常数：在 Mertens 显式阈值和行/列误差阈值成立后，可取 `(1,1,1/8,1)`。因此 `C_bd` 的最终吸收也并入 7.5.6 的机械阈值检查。剩余硬点进一步压缩为：`C_Rank` 的 Rankin 权 `σ`，以及行 B.0.4* 误差函数 `E_row(P)` 与列端点误差函数 `E_col(P)` 的显式化。

## C_Rank 显式闭合进展

本轮将 `C_Rank` 从阻塞项移出：引理 2.6c 改用 Euler--Rankin 指数级数尾部，得到可取 `C_Rank=16`。阈值计算器的原子常数现在已全部有候选值；剩余不能直接声明最终闭合的原因转为行 B.0.4* 误差函数 `E_row(P)` 与列端点误差函数 `E_col(P)` 仍需显式阈值化。

## 尾界阈值试算结果

填入全部原子候选常数后，阈值计算器已可运行，但当前保守常数给出 `C_USC≈4.8e10`，在 `log P≤100000` 内仍找不到尾界阈值。故逻辑抽取链条比之前更完整，但“可执行有限验证”仍受常数爆炸阻碍。剩余核心不再是 `C_Rank` 空缺，而是需要压缩 `C_edge/C_USC` 或改进尾界提取，使 `P_*` 落到可验证范围。

## 阈值可控化诊断

本轮增强阈值计算器后得到定量结论：当前保守常数给出的机械尾界约为 `log P≈1.23e13`。主瓶颈是行整数化项：`C_USC≈4.8e10` 导致 `c_row≈1.63e-13`，而 `d_row=2` 给出 `log P≳d_row/c_row`。进一步反推表明，在 `log P≤10^6` 的目标范围内，即使 `C_USC=1` 也不能通过当前 `P^2` 并合整数化。因此真正的阈值可控化方向不是继续微调 `C_Rank`，而是减少行窗口并合维度、改用压缩参数族整数化或证明窗口簇扩散使坏簇数 `<1`。

## 簇级并合试算

B.4.1 已改写为簇级整数化，阈值计算器新增 `row_union_mode=cluster`。在 `A_row=20` 下，行整数化不再主导，阈值约为 `log P≈1.10e13`，瓶颈转为行高矩尾界本身。这一步排除了 `P^2` 并合造成的额外爆炸，但当前 `C_USC≈4.8e10` 仍使阈值不可执行。下一步必须优化 `C_USC` 的合成公式或改进 7.3 的 Markov 尾界提取。

## C_USC 线性合成进展

本轮新增线性合成模式：有限个 `(C_i r)^{C_i r}` 损失相乘时指数常数应相加，而不是平方放大。采用 `usc_mode=linear` 后，当前 `C_USC=2460`，簇级并合阈值约为 `log P≈256000`。这仍不可直接有限验证，但已比 `exp(1e13)` 级阈值大幅改善。剩余可控化方向是继续压缩 `C_edge`、改进 7.3 中 `kappa` 和 Markov 尾界安全损失，并显式化 `E_row(P),E_col(P)`。

## C_edge/C_tree 与 tiny 参数压缩

本轮继续压缩阈值：`C_edge=4`、`C_tree=16`，并采用优化 tiny 参数 `C_Cheb=2,A1=50`。阈值计算器当前给出 `C_USC=740`，机械尾界阈值约 `log P≈5.66e4`。相比 `log P≈2.56e5` 又下降约 4.5 倍。剩余主要瓶颈仍是行尾界本身；下一步应优化 7.3 的 Markov 安全损失和 `kappa` 选择。

## 直接 Markov 优化进展

阈值计算器新增 `tail_mode=direct`，按允许矩阶直接优化 Markov 指数。当前模板给出阈值 `log P≈32000`，较上一轮 `56647` 继续下降。报告显示行尾界和行整数化均有余量，新瓶颈是列整数化项。下一步应研究列方向是否也可簇级/结构化并合，或利用列容量余量减少必须并合的列异常族。

## direct 模式重复整数化修正

`tail_mode=direct` 已直接把行簇并合和列并合计入 Markov 指数。本轮去掉重复整数化检查后，阈值降至 `log P≈25212.7`。当前最佳行矩阶为 `m=1`，瓶颈变成低阶矩常数；后续优化方向是为二阶/低阶矩建立专门小常数，而非继续使用统一高矩 `C_USC=740`。

## 低阶矩专用常数诊断

新增 `low_moment_C` 诊断后，若能证明低阶矩专用常数约为 `40`，机械阈值可降到 `log P≈4504`。当前这仍是诊断模式，不是最终证明；需要补写二阶/低阶矩常数引理。剩余最有希望的压缩方向是单独证明行列筛余统计量的二阶方差或四阶矩常数，而不是沿用统一 `C_USC=740`。

## A.7.0 低阶矩枚举补写

低阶矩路线继续推进：A.7.0 已加入二点/四点枚举草案，并给出 `C_low=40` 的吸收框架。剩余审查点是把二点差分碰撞链、四点 connected 局部 Bell 类型和二阶 Euler 因子的常数逐项核验。若该核验完成，当前 `log P≈4504` 的阈值诊断可转为正式阈值账本的一部分。

## A.7.0 低阶矩常数核验表

`C_low=40` 已进一步从诊断接口升级为有限核验表。二点、pair--pair、三点块、四点树型 connected 与非骨架修饰均已有宽松吸收账本。剩余审查点不再是常数大小，而是低阶 connected 图型清单是否无遗漏。

## 低阶 connected 类型穷尽性

A.7.0 已补充低阶 connected 类型穷尽性：二阶只有差分链；四阶只有 pair--pair 或四点 connected，而四点 connected 又由树型 skeleton 加非骨架修饰穷尽。`C_low=40` 已升级为可赋值常数。剩余阈值障碍转向 `log P≈4504` 之前的有限验证不可执行，以及 `E_row(P),E_col(P)` 的显式阈值化。

## 有限验证 vs 继续压阈值路线判断

当前 `P_*≈exp(4504)` 级别，直接有限验证不可行。诊断表明压低 `C_low` 的收益有限，阈值下限仍在 `log P≈4000`；而优化 tiny 层参数、增大 `c0` 可把阈值降到 `log P≈1100`。所以应优先继续压阈值，具体攻 `c0=1/(20C_ChebA1)` 的保守因子：证明更锋利的 `θ(x)` 显式界、降低 tiny/near 分层 `A1`，并确认 tiny CRT 边界仍可吸收。

## tiny/c0 与低阶矩后续诊断

tiny 参数优化的理论收益上限约 `log P≈1071`；之后瓶颈转为 `C_low=40`。扫描表明把 `C_low` 压到 `20` 可到 `log P≈542`，压到 `10` 可到 `log P≈359`。所以下一步最优方向是精修二点/四点矩常数，目标 `C_low=20` 或 `10`，而不是设计有限验证或提高低阶 cutoff。

## tiny 层目标参数推进

A.4 tiny 层已改写为显式 `vartheta` 输入。目标参数 `C_Cheb=1.04,A1=16,S0=12` 可把阈值降到 `log P≈1071`，但需要证明 `vartheta(x)≤1.04x` 的适用阈值并复核 near 层分割不受影响。与低阶矩常数压缩结合后，有望进入 `log P≈500` 量级。

## 剩余硬点更新：tiny/c0 目标参数

`tiniy/c0` 方向的硬点已进一步缩小。现在目标参数 `C_Cheb=1.04,A_1=16,S_0=12` 的剩余义务只剩两类：

1. 给出正式的显式 `\vartheta(x)≤1.04x` 来源或有限核验表，使 `C_Cheb=1.04` 从安全目标值升级为默认值；
2. 将新增 tiny 初等不等式与最终 `P_*` 搜索器连接，确保低于阈值的小奇素数由有限验证覆盖。

near 层不再是独立阻塞点：`A_1=16,A_2=1024` 时素数倒数和保持绝对有界，损失已并入线性 USC 安全余量。

## Chebyshev 常数阻塞解除

`tiniy/c0` 路线中的 `C_Cheb` 阻塞已解除：Dusart 显式界 `\vartheta(x)-x<x/36260` 允许无条件取 `C_Cheb=1.04`，甚至 `1+1/36260`。因此 tiny 层不再需要额外大范围核验；小范围核验脚本仅保留为独立复核工具。剩余主硬点转为低阶矩常数 `C_low` 与行/列误差函数的最终显式阈值化。

## C_low=20 压缩进展

本轮在 A.7.0 后补入压缩版低阶矩账本：二点差分链用标准 `d_4` 平均 `\sum_{n≤X}d_4(n)\ll X\log^3X`，安全放大到 `\log^{20}P`；四点 connected 块在 `r=4` 时只有三条骨架边，非骨架额外 rank 由二阶 Euler 和吸收，不再需要 `\log^{80}P`。因此低阶矩专用常数可从 `C_low=40` 压到 `C_low=20`，对应机械阈值从 `log P≈1071.40` 降到约 `593.42`。下一步若继续压到 `10`，需要把二点安全放大从 `\log^{20}P` 再降至约 `\log^{10}P`，并更精确核算四点有限类型数。

## 当前最新阈值状态

当前最佳尾界模板为 `C_Cheb=1+1/36260,A_1=16,S_0=12,C_low=20`，机械输出 `log P_tail≈593.418328118502`。这仍远高于直接有限验证范围，且尚未合并行/列误差函数阈值；下一步最有效攻坚是尝试 `C_low=10`，或把尾界使用的低阶矩 Markov 常数进一步结构化。

## C_low=10 锋利账本接口

本轮进一步补入 `C_low=10` 的可证版本：二点链保留标准 `d_4` 平均的真实 `\log^3P`，将绝对常数放入显式闸门 `10^2\log^7P≥C_{d4}`；四点 connected 块只保留三条骨架边的 `\log^3P`，有限类型、叶剥离和非骨架二阶 Euler 尾并入常数 `C_{4,loc}`，由 `10^4\log^7P≥C_{4,loc}` 吸收。这样低阶矩形式升级为 `C_low=10`，机械阈值预计降到 `log P≈484.04`。剩余审查义务是给出 `C_{d4}` 与 `C_{4,loc}` 的显式数值，而不是再寻找新的 connected 图型。

## 当前最新阈值状态：`C_low=10`

当前最优尾界接口为 `C_Cheb=1+1/36260,A_1=16,S_0=12,C_low=10`，机械输出 `log P_tail≈484.03986414399714`。剩余不是 connected 类型遗漏，而是两个有限常数的数值化：`C_{d4}` 和 `C_{4,loc}`。下一步应显式估计这两个常数，或在脚本中加入这两个初等闸门后反算它们对最终阈值的影响。

## C_low=10 闸门数值化

本轮给出两个保守显式闸门赋值：`C_{d4}=10^4`，`C_{4,loc}=10^8`。对应初等条件分别为 `10^2\log^7P≥10^4` 与 `10^4\log^7P≥10^8`，即 `\log P` 只需大于约 `2` 与 `4`，在当前尾界阈值 `log P≈484.04` 处自动成立。阈值脚本已加入 `C_d4_gate`、`C_4loc_gate` 两项并检查余量。因此 `C_low=10` 可从“待数值化接口”升级为“保守可赋值常数”，剩余全局障碍回到 `E_row(P),E_col(P)` 与最终有限验证。

## `E_row/E_col` 剩余障碍压缩

行列误差现在已转化为四个可替换原子参数：`C_row_err,A_row_err,C_col_err,A_col_err`。当前模板 `10^12,8,10^12,8` 不抬高 `log P≈484.04` 的尾界。剩余真正障碍不再是阈值脚本接口，而是 B.0.4\* 的最终审稿常数抽取：必须证明 sawtooth 相消、DBA/resultant、高度传播和 dyadic/Fourier 并合后的误差确实满足该对数幂模型。

## B.0.4\* 剩余硬点再压缩

B.0.4\* 的常数抽取现在已变成脚本化原子账本：`C_*=160`，`B1,B2,B4,B5` 余量通过，行列误差模板不抬高当前阈值。剩余真正硬点是 6.14 坏层生成元覆盖审查：必须确认分母、导数、ramification、四点 rank 失效、Jacobian 层和步长共振层覆盖 6.7--6.18 中所有退化来源。

## 坏层覆盖闭包 2.0

6.14.2c 已把 B.0.4\* 的原始边界来源、LV/AE 反推中间退化与 resultant atlas 放入同一张表。当前剩余障碍从“是否存在第七类坏层”压缩为“逐行核对每个正规化对象的公式是否已在 6.17/6.18 定义并满足 6.14 atlas 的次数/高度假设”。

## `b(a)` 最小剩余硬点闭合

B.0.3e+ 已补齐 CRT 叶剥离剩余类 `b(a)` 到 6.17 admissible 有理分片的映射。当前剩余障碍再压缩：不再是对象 admissibility，而是最终全局层面的 `P_*` 合并、有限验证策略，以及所有历史追加段落中旧状态的清理/定稿一致性复核。

## 当前已脚本化最终账本

当前脚本化合并阈值为 `log_P_scripted≈484.03986414399714`，涵盖 tail、tiny、low-moment、row/col error、B.0.4\* parameter gates。尚未纳入脚本的最终障碍是：`P_M`、`P_G`、`P_cap` 的具体显式来源，B.0.4\* 覆盖矩阵最终人工审稿确认，以及 `P≤exp(484)` 的有限验证不可执行性。下一步应优先形成“充分大奇素数定稿版”与“全部奇素数仍需降阈值/新有限验证策略”的双层结论。
