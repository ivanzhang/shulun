# 最终证明审稿记录

> **最新状态以本节为准。** 下方大量段落是按时间追加的审稿日志，含历史阈值和旧阻塞项；定稿判断以本“最新审稿总表”为准。

## 0. 最新审稿总表

| 接口 | 最新审稿状态 |
|---|---|
| 主结论层级 | 当前诚实定稿为“充分大奇素数”；“全部奇素数”为条件升级 |
| 硬窗口主线 | B.0.4* admissibility、坏层覆盖、参数账本均已结构化 |
| `b(a)` admissibility | B.0.3e+ 已补齐 CRT 叶剥离到有理分片的映射 |
| 常数脚本 | `--final-ledger` 输出 `log_P_scripted≈484.03986414399714` |
| 小范围验证 | `verify_finite_p_grid.py --max-p 1000 --quiet` 通过 |
| 不可声称事项 | 尚未完成 `P≤P_*` 的全范围有限验证，不能声称所有奇素数已无条件证明 |

## 0.1 下一步定稿任务

1. 把旧历史段落移入“研究日志”或删减，只保留最新总表。
2. 给 `P_M,P_G,P_cap` 补正式引用和数值。
3. 继续降低 `log_P_scripted` 或设计不可枚举区间的验证替代策略。


---

## 历史记录区

以下内容为研究过程中按时间追加的复核/障碍记录；若与顶部最新总表冲突，以顶部最新总表为准。

日期：2026-04-28

## 审查结论

`docs/final-proof-draft.md` 当前已从早期“条件归约稿”推进为硬窗口 `B.0.4*` 主线的最终审稿复核稿。核心 sawtooth 相消链条已经被拆成：admissibility、LV 大值反推、AE 能量反推、四点 rank、离散 coarea、DBA/resultant atlas、FGH 高度账本和参数余量。

严格数学审查下，仍不应在尚未抽取显式阈值 `P_*` 前声明“所有奇素数”版本已经完成。当前可声明的是：硬路线的结构性缺口已压缩为有限审稿表；理论主线给出“所有充分大奇素数”的证明框架，最终全体版本还需显式阈值与有限验证。

## 已闭合或已结构化部分

1. 圆柱行移动被写成 CRT 平移函数方程。
2. 全覆异常被反推为端点 sawtooth/Fourier 锁相异常。
3. `B.0.4*` admissibility 已由 `Q,b(a),W(a),N,Y` 来源表核查。
4. `LV` 已拆成加权 vdC、大相关步长、短弧集中，并补齐 `W_h` 变差与非共振密度。
5. `AE` 已拆成短弧层化、`M^4/L^3` 能量下界、层数二分。
6. coarea/Jacobian/ramification 坏层已列入 resultant atlas，并补充 atlas 穷尽性反证。
7. FGH 高度账本与坏素权重闭合已写成统一引理。
8. `LV/AE/coarea/DBA` 全部对数损失已纳入 `6.15` 参数余量表。

## 仍需最终审稿复核

1. 逐项核对 `6.14.2a` resultant atlas 清单确无遗漏。
2. 逐项核对 `6.14.3a--6.14.4a` 高度/resultant 标准界和 Rankin 常数。
3. 复核列方向坏列整数化与容量余量。
4. 从全文常数抽取显式阈值 `P_*`，并运行完整有限验证。

## 归档文件

- 当前工作稿：`docs/final-proof-draft.md`
- 本次归档副本：`docs/archive/final-proof-draft-2026-04-28.md`
- 本审查报告：`docs/final-proof-review.md`

## 验证状态

已运行有限数值验证：

```bash
python3 experiments/verify_finite_p_grid.py --max-p 1000 --quiet
```

结果：`SUMMARY: all passed for 167 odd primes P<= 1000`

已运行文档一致性检查：公式块平衡、无控制字符、无 tab、无 DEL、代码围栏平衡。

## 6.9 高维动力系统再审查补充

后续严查把 `UAS/DBA` 进一步提升为高维动力系统问题：短弧集中等价于低次数有理相位轨道在圆周薄管中的低熵异常。文档新增 `6.9` 节，将无条件闭合所需的新输入明确为二选一：

1. 短弧有理大筛；
2. 四点能量定理；

并要求 `DBA-closure` 统一吸收差分、导数与四点能量产生的全部判别式。该补充强化了归约结构，但也确认当前仍不是无条件证明。

## 6.10 四点能量路线审查补充

新增 `6.10` 节把四点能量路线拆为对角、半对角、真四点三类。局部 rank 界可由 Schwartz--Zippel 在有效素因子上给出；真正剩余接口被进一步明确为：

1. `4E-ND`：四点多项式非退化，所有恒等或大维因子簇进入 `DBA-closure`；
2. `4E-DISP`：从模 `Q'` 零集界到实短弧厚化解数的离散转换；
3. `DBA-closure`：统一吸收四点能量、导数、差分产生的判别式。

因此四点路线进一步强化了归约，但仍未构成无条件证明。

## 6.11 Jacobian/DBA-closure 审查补充

新增 `6.11` 节把 `4E-DISP` 进一步归约为 Jacobian 非退化 `JND` 与有限生成高度账本 `FGH`。该节说明：厚化离散转换失败会迫使四点场与其梯度同时退化；若 Jacobian 退化簇仅来自 Möbius/仿射/常值及分母导数坏层，则可由 `DBA-closure` 吸收。当前仍需证明 `JND` 的代数几何分类和 `FGH` 的统一高度可和性，因此仍是条件归约。

## 6.12 JND/FGH 函数方程化审查补充

新增 `6.12` 节把 `JND` 进一步转化为有理函数四点奇异分类 `JND-class`，并把 `FGH` 转化为有限次代数操作下的标准高度传播账本。当前最接近闭合的剩余点是严格证明 `JND-class`：非退化一维有理函数的四点 Jacobian 三维奇异分支只能来自仿射、一次分式或已列账退化。`FGH` 看起来可由标准高度/resultant 估计闭合，但仍需在附录中严写。

## 6.12 JND 修正审查补充

后续严查修正了 `JND-class` 的几何表述：普通四点水平面本来就是三维，不能被误判为退化。新的正确对象是临界纤维 `JND-crit`：只有 `F=0` 同时 Jacobian/投影 Jacobian 退化时，才会造成厚化层超出 coarea 预测。该修正把剩余核心改为离散 coarea 估计与 ramification/resultant 坏层吸收，而非分类所有三维水平面。

## 6.13 离散 coarea 审查补充

新增 `6.13` 节把 `4E-DISP` 的厚化离散转换归约为一变量有理函数分片估计：固定三个变量后，非临界单调段贡献 `O(1+nu A^{1+o(1)})`，对三变量求和得到 `O(nu A^{4+o(1)}+A^{3+o(1)})`。小导数、分母极点与 ramification 层交由 `DBA-closure` 吸收。当前最后障碍进一步集中到 DBA-closure 的完全严写。

## 6.14 DBA-closure 账本化审查补充

新增 `6.14` 节列出 DBA-closure 的坏层生成元：分母层、导数层、ramification 层、四点 rank 失效层、Jacobian 层、步长共振层。文档给出有限生成高度界与 connected Rankin 账本吸收方案。当前剩余审查点是：逐项确认生成元清单覆盖 6.10--6.13 中所有退化，并核对 `B_1,B_2,B_4` 的参数余量大于全部高度传播和 Rankin 损失常数。

## 6.15 参数余量核查表审查补充

新增 `6.15` 节统一定义最大对数损失常数 `C_*`，并给出显式参数选择：

- `B_4=4B+4C_*+100`
- `B_2=6B+4C_*+120`
- `B_1=8B+4C_*+160`

该表覆盖 Fourier 截断、UAS 短弧少根密度、van der Corput 初项、步长共振、DBA 坏素因子、离散 coarea 厚化、dyadic/Fourier 并合与 Stieltjes 权转换。当前参数余量已不再是独立结构障碍；最后审查点是 6.14.1 坏层生成元清单是否覆盖所有退化。

## 6.14.2 坏层覆盖矩阵审查补充

新增 `6.14.2` 覆盖矩阵，逐项对照 `6.10--6.13` 的退化来源：四点分母、rank 失效、水平纤维厚化、导数消失、Jacobian 退化、临界纤维、ramification、步长共振等。除对角/半对角已单独计数外，所有退化均映射到 `6.14.1` 六类生成元。至此，坏层覆盖问题被压缩为有限清单审查。

## 6.16 最终闭合审查发现

最终反向审查发现一个关键接口一致性问题：`UAS/DBA`、四点能量、离散 coarea 与坏层账本闭合的是几何端点 sawtooth 相消，适用于硬边界 `B.0.4*` 路线；它们并不自动推出带 `dπ(p)` 的素数权 `B.0.4S-short`。若选择平滑路线，仍需独立的素数权 Type-I/AP 双线性定理。若选择硬路线，则应统一主证明依赖为 `B.0.4*`，并复核其余局部容量估计。当前不能声称 `B.0.4S-short` 已由 UAS/DBA 无条件闭合。


## 6.17 B.0.4* 硬路线覆盖矩阵审查补充

新增 `6.17` 节后，主线从平滑素数权接口切回硬窗口 sawtooth 接口。文稿现在把 `B.0.4*` 明确表述为：所有行窗口端点、dyadic 盒端点、叶剥离端点与曲线边界端点，要么规范化为有限个 sawtooth 有理相位和，要么进入 `DBA-closure` 的六类坏层。覆盖矩阵逐项对应左端点、右端点、端点差分、盒截断、叶剥离分片、分母层、导数层、四点厚化层、Jacobian/ramification 层与步长共振层。

审查结论更新为：若 `6.7--6.15` 的 UAS、四点能量、离散 coarea 与 DBA-closure 严格证明逐项通过，则 `B.0.4*` 可由硬路线闭合，行命题不再依赖 `B.0.4S-short`。平滑素数权路线仍作为备选，但不能由 UAS/DBA 自动推出。最终审稿的唯一核心核查变为：附录 B 产生的所有 `W(a),b(a),Q` 是否满足 `6.17` 的 admissibility，且非 admissible 分片是否全部落入 `6.14` 六类坏层。


## 最终未处理事项决策

新增 `docs/final-closure-obstacles.md` 作为最终硬障碍与文件处理清单。当前应纳入证明稿审查的是 `docs/final-proof-draft.md`、`docs/final-proof-review.md` 与 `docs/final-closure-obstacles.md`。`docs/rigid-patch-lemma-experiments.md`、`experiments/rigid_patch_lemma_scan.py` 以及 134 个未跟踪实验脚本暂不混入主证明提交；它们属于探索性实验资料，应后续按主题分批整理、补充用途说明后再提交。

最终闭合不能靠实验日志替代证明。下一轮最优先任务是以 `6.17` 为索引，逐项反查附录 B 的 `W(a),b(a),Q` admissibility，并把所有失败分片映射到 `6.14` 六类坏层。


## 6.17.6--6.17.7 admissibility 核查补充

本轮继续反查附录 B：`Q` 来自小筛 squarefree CRT 模数，`b(a)` 来自可逆剩余类和叶剥离 CRT 合成，`W(a)` 来自 Stieltjes 卷积测度与 dyadic 截断。因此正常主盒满足 `6.17.1` 的 admissibility；失败情形进入小筛剔除、低体积盒或 `DBA-closure` 坏层。

同时文稿明确区分两个层级：对象核查已闭合，但 sawtooth 相消仍需 6.7--6.15 的 UAS/FNL、四点能量、离散 coarea 与 DBA-closure 链条独立证明。该修正避免把接口规范化误写成无条件相消证明。


## 6.18 sawtooth 相消链条严写补充

新增 `6.18` 节把硬窗口 `B.0.4*` 的 sawtooth 相消拆成三段：`LV` 大 Fourier 值反推短弧集中，`AE` 短弧集中反推四点厚化能量异常，最后由四点 rank、离散 coarea 与 `DBA-closure` 排除异常。该节还把旧的 `FNL/NL => B.0.4S-short` 主线修正为 `FNL/NL => B.0.4*`，平滑素数权接口继续只作为备选路线。

当前最后审稿点进一步缩小为：复核 6.14.2a 的 resultant atlas 是否覆盖所有 coarea/Jacobian/ramification 坏层。参数表已纳入 LV/AE 新增损失。


## 6.18.2 AE 层化能量严写补充

本轮专攻 `AE`：把原先粗略的 `M^4/L` 四点能量下界修正为安全的层化 Holder 下界 `M^4/L^3`。新增短弧层化定义、层化四点能量引理和严写版 `AE` 命题。结论是：若 `L=O(1)` 或 `L\le\log^{O(1)}P`，该损失可由参数余量吸收；若某分片导致 `L` 超过对数幂，则它不再是组合能量问题，而必须作为分母、导数或 ramification 坏层进入 `DBA-closure`。

这次修正提高了逻辑严谨性：短弧集中并不无条件给出过强能量下界，而是经层化后给出可审查的厚化四点异常。


## 6.18.1 LV 加权 van der Corput 严写补充

本轮专攻 `LV`：将原先概略的“大 Fourier 值反推短弧集中”拆成三个标准引理：加权 van der Corput、大值给出大相关步长、相关大值反推短弧集中。新增了权重总变差 `Var(W)` 的显式账本，说明 Stieltjes 卷积权在 dyadic 正常盒内只带来对数幂损失，由 6.15 参数余量吸收。共振步长 `q|h`、`q|mT` 与分母不可逆层继续交由 6.14 的步长共振层和分母层处理。

这使 `LV` 从直觉性反推变成可审查链条；本轮后 `W_h` 的有界变差继承与共振步长剔除已被写成独立微引理，剩余只需在最终审稿中核对其参数常数。


## 6.18.1d--e 权重继承与非共振密度补充

本轮继续排除 `LV` 的两个最小硬点：新增 `W_h` 变差继承引理和非共振步长密度引理。`W_h(a)=W(a+h)W(a)` 的变差由乘积变差不等式控制，保持对数幂损失；坏步长由 `q|h`、`q|mT` 和分母不可逆因子生成，在步长平均中贡献 `O(1/q+1/H)`，由 6.14 的 DBA-closure 与 6.15 的 `B_4` 余量吸收。

因此 `LV` 的剩余问题不再是结构性缺口，而是最终参数常数复核。


## 6.18.2b AE 层数二分补充

本轮专攻 `AE` 的层数 `L`：新增“层数二分”引理，说明正常 dyadic 盒中 `F_t=N_t/D_t` 的短弧可分辨层数满足 `L≤log^{C_L}P`；若层数失控，则必然来自分母极点、小导数或 ramification/resultant 分支合并，分别进入 6.14 的分母层、导数层和 ramification 层。因此 `L^3` 损失在正常层中只是对数幂，可由 6.15 参数余量吸收。

这一步把 `AE` 的层化损失从剩余硬点降为 DBA 覆盖矩阵的一项核查。


## 6.14--6.15 最终覆盖矩阵与参数表补充

本轮把 `6.18` 新增退化源纳入 `6.14.2` 覆盖矩阵：`W_h` 阶梯端点、非共振步长剔除、`L` 层数失控、层化四点厚化异常分别映射到 dyadic/低体积账本、步长共振/分母层、分母/导数/ramification 层、四点 rank/Jacobian/coarea 坏层。`6.15` 参数表同步加入 `C_LV` 与 `C_AE`，并把 `L^3`、`W_h` 变差、步长平均等损失纳入 `C_*`。

至此 `LV` 与 `AE` 不再作为独立结构缺口；最后核心审查点集中到 `6.13--6.14` 的 coarea、Jacobian、ramification、resultants 坏层覆盖是否真的无遗漏。


## 6.14.2a resultant atlas 补充

本轮新增 `6.14.2a resultant atlas`，把最后的 coarea、Jacobian、ramification、resultants 坏层写成有限生成清单：分母极点、一变量导数、四点投影 Jacobian、水平纤维判别式、分支合并 resultant、四点 rank 失效、Jacobian 公共分支、LV/AE 层化失控、步长/频率共振。

文稿现在给出无遗漏审查原则：若某坏情形不在 atlas 中，则它同时满足分母非零、导数非零、投影 Jacobian 非零、无 ramification、局部 rank 有效、步长非共振；在这些条件下 coarea、局部 rank 与 LV/AE 链条已经给出预算内估计。因此最后核心审查被压缩为复核 atlas 清单本身。


## 6.14.2b atlas 穷尽性补充

本轮新增 `6.14.2b atlas 穷尽性`。证明采用反设：若存在 atlas 外坏机制，则它必须同时满足分母非零、导数/Jacobian 非零、无 ramification、rank 有效、步长非共振；在这些正常条件下，`LV => AE => 四点 rank/coarea/参数余量` 已给预算内估计，矛盾。因此不存在 atlas 外“第七类退化”。

同时修正了 `6.14.3` 中重复的 resultant 高度公式。当前最后状态是：结构性坏层已压缩为 atlas 清单复核与有限生成高度账本复核。


## 6.14.3a--6.14.4a FGH 高度与坏素权重补充

本轮专攻有限生成高度账本：新增 `6.14.3a FGH 统一高度账本`，逐项列出 atlas 生成元的操作、次数与高度传播；新增 `6.14.4a 坏素权重闭合`，证明固定 skeleton 下所有坏层多项式的坏素因子权重仅为对数幂，并可并入 connected Rankin 账本和 `C_*` 参数余量。

这把“有限生成高度账本复核”从结构性缺口降为标准高度/resultant 与 Rankin 常数核查。


## 6.19 最终审稿级复核表补充

新增 `6.19` 后，文稿给出最终审稿级状态表：`B.0.4*` admissibility、LV、AE、四点 rank、离散 coarea、resultant atlas、FGH、高度账本与参数余量均已结构化；剩余为 atlas/高度常数复核、列方向常数复核，以及显式阈值 `P_*` 与有限验证。因此当前严格结论应表述为“所有充分大奇素数”的硬路线证明框架；“所有奇素数”版本需等待显式阈值和有限验证。


## 再次审稿级复核与扩展有限验证

本轮复核确认：当前文稿尚未抽取显式理论阈值 `P_*`，因此无法在严格意义上完成“充分大之前所有奇素数”的有限验证。已执行更高范围的可复现实证验证：

```bash
/usr/bin/time -f 'elapsed=%E maxrss=%MKB' python3 experiments/verify_finite_p_grid.py --max-p 10000 --quiet
```

结果：`SUMMARY: all passed for 1228 odd primes P<= 10000`，耗时约 `0:29.98`，峰值内存约 `1566592KB`。

审稿结论：该结果强化了小范围现象验证与脚本可信度，但不能替代定理 6.2 所要求的 `P≤P_*` 完整验证。最终无条件“所有奇素数”版本仍需先从理论证明中显式抽取 `P_*`，再运行 `--max-p P_*`。


## 显式阈值 P_* 抽取账本补充

本轮按审稿要求尝试抽取 `P_*`，结论是：当前文稿尚未数值化所有 `o(1)`、`O(1)`、`P^{o(1)}`、`\ll` 与“足够大”常数，不能诚实给出实际 `P_*`。已在 `docs/final-proof-draft.md` 新增 `7.2 显式阈值 P_* 抽取账本`，列出必须显式化的八类阈值：USC 高矩、行候选下界、行安全间隙、`B.0.4*`、参数余量、列候选主项、列容量余量、坏列整数化。

当前已完成 `P_{max}=10000` 的扩展验证，但这不能替代 `P≤P_*` 的最终有限验证。


## 7.2 常数数值化进展

本轮对 `P_*` 抽取账本做了第一步数值化：行安全间隙常数 `e^γ/4 log 3 = 0.4891770114`，加上 `θ_3=0.42` 得 `0.9091770114`，可保守取 `η=0.08`；列容量常数 `e^{-γ}=0.5614594836`，而 `2(1-0.85)=0.3`，绝对余量 `0.2614594836`，相对余量约 `0.4656782746`。

同时文稿新增阻塞表：`P_A,P_H,P_G,P_B,P_par,P_C,P_cap,P_int` 仍需显式化各自的 `o(1)`、`O(1)`、`P^{o(1)}`、`\ll` 常数。故本轮仍不能诚实给出实际 `P_*`。

## 7.3 高矩尾界显式参数化补充

本轮新增 `7.3 高矩尾界常数的显式参数化`。它把 USC 高矩界到异常概率的 Markov 转换完全写成参数公式：输入 `C_USC,c_0,μ_0,β,a,δ` 后，可计算 `κ,c_tail`，并得到行窗口 `c_row=κ_row/8`、列场 `c_col=κ_col/4` 以及整数化条件 `P^{d_row}exp(-c_row(log P)^2)<1`、`P exp(-c_col(log P)^2)<1`。

审稿状态因此更新为：尾界转换环节本身已从非定量 `exp(-c(log P)^2)` 改成显式参数不等式；剩余硬点集中在输入常数的数值化，即附录 A 的 `C_USC,c_0`、行/列主量下界 `μ_row,a_row,μ_col,a_col`，以及 B/C 容量误差常数。

## 7.4 原子常数账本与阈值计算器

本轮新增 `7.4 原子常数抽取账本与有限验证闸门`，把最终 `P_*` 抽取拆成有限原子常数：`C_Bell,C_tree,C_edge,C_Rank,C_leaf,C_bd,C_Cheb,A1` 以及行/列主量常数。并新增 `experiments/explicit_threshold_ledger.py`，用于在这些常数全部赋值后机械计算尾界阈值。

审稿边界：该脚本不会伪造未知常数；若常数 JSON 中存在空值，会直接拒绝计算。因此当前推进结果是“显式抽取流程和计算闸门完成”，而不是“最终数值 `P_*` 已完成”。

## 显式常数赋值审计

本轮新增 `docs/explicit-constant-audit.md`。审计结论是：`C_Cheb=8`、`A1=100`、`delta_row=delta_col=0.1`、`d_row=2` 可以作为保守外围参数；但 `C_Bell,C_tree,C_edge,C_Rank,C_leaf,C_bd,mu_row,a_row,mu_col,a_col` 仍缺少文稿内的数值化证明，不能诚实赋值。因此最终 `P_*` 仍未可计算，最终有限验证不能启动。

## C_Bell 与 C_tree 显式赋值进展

本轮新增引理 2.5a 与 2.6a：Bell--Möbius 权和局部 Bell 选择可由 `C_Bell=16` 控制；有效 rank 区域 `R≤8r` 的 skeleton 来源选择可由 `C_tree=64` 控制。`docs/explicit-constant-audit.md` 已同步把这两项移入可赋值清单。注意：`C_tree=64` 只覆盖有效 rank 区，高 rank 尾部仍归 `C_Rank`，尚未闭合。

## C_edge 与 C_Rank 判据进展

本轮新增引理 2.6b 与 2.6c。引理 2.6b 用逐 rank 局部编码给出 `W_q(ρ)≤(128r)^{128ρ}/q^ρ`，因此 `C_edge=128` 可赋值。引理 2.6c 说明高 rank 尾部不能仅靠当前粗局部计数和 `q^{-2}` 自动闭合，而必须显式引入 Rankin 权 `σ` 并验证 `σΞ_r≤1/2`。因此 `C_Rank` 的阻塞被精确化为一个数值 Rankin 权判据，而不是泛泛的“足够大”。

## C_leaf 与 C_bd 进展

本轮新增引理 2.8a，并在 tiny 层下加入边界显式判据。固定局部项的叶剥离端点损失可由 `C_leaf=8` 控制；tiny CRT 不完整周期的局部边界倍数可取 `C_bd=4`，但其最终阈值仍需代入行/列主量常数检查 `4M_0≤(1/4)μ_0P^β/log^aP`。因此 `C_leaf` 已闭合，`C_bd` 的局部倍数已闭合，剩余耦合到 `mu_row,a_row,mu_col,a_col`。

## 行列主量常数接口进展

本轮新增 `7.5 行列主量常数的显式接口`。使用保守 Mertens 乘积输入 `∏_{q≤x}(1-1/q)≥1/(4log x)` 后，行窗口主量可条件取 `mu_row=1,a_row=1`，列场主量可条件取 `mu_col=1/8,a_col=1`。这些取值不是无条件最终赋值；它们要求最终阈值同时验证行误差条件 (7.5.2)、列误差条件 (7.5.4) 和 tiny 边界 (7.5.6)。

## C_Rank 显式闭合进展

本轮重写引理 2.6c：不再把高 rank 尾部留给未知 Rankin 权 `σ`，而是采用逐素数 Euler 乘积后的指数级数记账。额外 rank 的无序标签尾部由 `sum_h (2r)^h/h! = e^{2r}` 控制，并可并入 `(16r)^{16r}`，因此可取 `C_Rank=16`。审计表与阈值模板已同步填入该值。

## 尾界阈值试算结果

在填入 `C_Rank=16` 后，`experiments/explicit_threshold_ledger.py` 已能进入完整计算流程。派生常数极保守：`C_USC≈4.80091921e10`、`c0=6.25e-5`，导致在 `log P≤100000` 内尾界条件和整数化条件仍未同时满足。该结果不是逻辑反例，而是说明当前显式常数过粗，最终有限验证阈值若按这些常数机械抽取会不可执行；后续若要实际跑完 `P≤P_*`，必须优化 `C_edge/C_USC` 或改用更锋利的高矩尾界常数。

## 阈值可控化诊断

本轮增强 `experiments/explicit_threshold_ledger.py`，加入二分阈值搜索、条件余量报告和目标 `log P` 反推。用当前保守常数试算，尾界阈值约为 `log P≈1.22903531776e13`，即 `P≈exp(1.229e13)`。瓶颈不是单一 `C_Rank`，而是：`C_USC≈4.8e10` 使 `c_row≈1.63e-13`，行整数化条件 `P^{d_row}exp(-c_row(log P)^2)<1` 强迫 `log P` 至少约 `d_row/c_row≈1.23e13`。

反推工具显示，即使把 `C_USC` 降到 `1`，若保持当前行并合维度 `d_row=2` 和尾界形式，小目标 `log P≤10^6` 仍无法通过整数化条件。因此阈值可控化必须同时做两件事：压低并合维度/避免 `P^2` 逐窗口并合，或把尾界从当前全局 Markov 形式升级为更强的确定性/簇级整数化；仅优化 `C_Rank` 不够。

## 簇级并合试算

本轮把 B.4.1 改写为簇级整数化接口，并让阈值计算器支持 `row_union_mode=cluster`。取 `A_row=20` 后，行整数化余量已不再是瓶颈；机械阈值从逐点并合的 `log P≈1.229e13` 降到约 `log P≈1.10009e13`，此时瓶颈变为行尾界本身 `row_tail_margin=0`。这说明簇级并合方向正确，但若要把阈值降到可执行范围，还必须优化高矩尾界公式或大幅压缩 `C_USC`。

## C_USC 线性合成进展

本轮将 `C_USC` 的合成从极保守平方公式 `100(C_Euler+1)^2` 增补为乘积指数直接合成公式 `10(C_Bell+C_tree+C_edge+C_Rank+C_leaf+C_bd+10)`。阈值计算器新增 `usc_mode=linear`。在当前候选常数、簇级并合 `A_row=20` 下，`C_USC` 从约 `4.8e10` 降为 `2460`，机械尾界阈值从约 `log P≈1.10e13` 降为 `log P≈256000`。这说明主要爆炸确实来自合成公式过粗；后续可继续优化尾界提取和列整数化常数，使阈值进一步下降。

## C_edge/C_tree 与 tiny 参数压缩

本轮压缩两个组合常数：引理 2.6b 将 `C_edge` 从 `128` 降为 `4`；引理 2.6a 将有效 rank 区 `C_tree` 从 `64` 降为 `16`。同时模板采用优化 tiny 参数 `C_Cheb=2,A1=50`，该选择需由显式 Chebyshev 界和 tiny 阈值检查支撑。试算显示：在 `usc_mode=linear,row_union_mode=cluster` 下，默认优化模板给出 `C_USC=740`，机械尾界阈值约 `log P≈56647.4`。

## 直接 Markov 优化进展

本轮新增 `tail_mode=direct`：不再固定 `m=floor(kappa log P)` 并强迫括号小于 `P^{-β/2}`，而是在 `1≤m≤c0 log P/2` 内直接最小化 Markov 指数。该模式仍使用同一高矩不等式。当前优化模板下，机械尾界阈值从 `log P≈56647.4` 降到 `log P≈32000`；此时行尾界已有正余量，瓶颈转为列整数化边界。

## direct 模式重复整数化修正

本轮修正阈值计算器：`tail_mode=direct` 的 `row_tail_margin` 与 `col_tail_margin` 已经扣除对应并合数量，不应再额外要求旧的二次整数化条件。修正后当前模板阈值从 `log P≈32000` 降为 `log P≈25212.7`。报告显示瓶颈重新回到行尾界，且最佳矩阶为 `m=1`，说明下一步应单独优化低阶矩/二阶方差常数，而不是继续依赖统一 `C_USC=740` 的高矩常数。

## 低阶矩专用常数诊断

本轮阈值计算器新增 `low_moment_C` 诊断：当直接 Markov 优化选到低矩阶时，可用未来需证明的低阶矩专用常数替代统一高矩常数。以诊断值 `low_moment_C=40, low_moment_cutoff=2` 试算，阈值降至 `log P≈4503.91`。这不是已完成证明，因为还需严写二阶/低阶矩的专门常数；但它明确指出最有效的下一步是证明低阶方差/四阶矩小常数，而不是继续优化高阶 connected 账本。

## A.7.0 低阶矩枚举补写

本轮在 A.7.0 中补入二点和四点枚举草案：二阶矩只需处理差分碰撞链；四阶矩分为 pair--pair、三点加单点、四点 connected 三类。文稿给出 `C_low=40` 的宽松吸收形式，但仍标注需最终逐项核对局部 Bell 类型数和二阶 Euler 因子显式常数。因此 `C_low=40` 已从纯诊断推进为可审查证明草案，尚未完全升级为无条件定理。

## A.7.0 低阶矩常数核验表

本轮把 `C_low=40` 从证明草案进一步推进为有限核验表：二点差分链、pair--pair、三点块+单点、四点树型 connected、四点非骨架修饰均给出类型数上界、Euler/叶剥离损失和吸收方式。审计状态已更新为“条件可用”：只要最终确认低阶 connected 类型无遗漏，`C_low=40` 可作为正式低阶矩常数使用。

## 低阶 connected 类型穷尽性

本轮在 A.7.0 中补入低阶 connected 类型穷尽性证明。二点情形唯一为差分碰撞链；四点 moment--cumulant 分区在去掉单点块后只剩 `2+2` 与 `4`，其中 `2+2` 是 pair--pair，四点 cumulant 块由树型 skeleton 加非骨架修饰穷尽。因此不存在第六类低阶 connected 图型。审计表已将 `C_low=40` 升级为可赋值。

## 有限验证 vs 继续压阈值路线判断

当前机械阈值 `log P≈4503.91`，即 `P≈exp(4504)`，远超任何直接有限验证能力。因此“设计更强有限验证”不是当前优先路线，除非先把理论阈值降到至少可枚举范围。诊断显示继续压 `C_low` 收益很小：即使从 `40` 降到 `2`，阈值也只到 `log P≈4033`。真正有效的是提高允许低阶矩阶范围，也就是增大 `c0`：若把 tiny 参数从 `C_Cheb=2,A1=50` 优化到接近 `C_Cheb=1,A1=16`，阈值约降到 `log P≈1103`；若 `A1≤12` 且低阶 cutoff 到 `m=2` 可用，阈值约为 `log P≈1071`。因此下一优先突破点是 tiny 层/c0 优化，而不是有限验证。

## tiny/c0 与低阶矩后续诊断

本轮扫描显示：仅优化 tiny 层可把阈值从 `log P≈4504` 降到约 `1071`，但随后受 `low_moment_C=40` 限制。提高低阶 cutoff 到 4--6 阶收益很小；真正有效的是压低低阶矩常数：若 `C_low=20`，阈值约 `log P≈542`；若 `C_low=10`，阈值约 `log P≈359`。因此下一步应优先精修二点/四点低阶矩常数，而不是扩展到更高阶低矩。

## tiny 层目标参数推进

本轮把 tiny 层写成显式 `vartheta(x)` 输入：`log M0=vartheta(A1 r)≤C_Cheb A1 r`。目标优化组合为 `C_Cheb=1.04,A1=16,S0=12`，需要证明相关范围内 `vartheta(x)≤1.04x` 并把小范围并入有限验证。试算：当前默认约 `log P≈2888`；目标 tiny 参数可降到 `log P≈1071`；若同时把 `C_low` 压到 `20`，约 `593`；压到 `10`，约 `484`。

## tiny/c0 显式化复核补充

本轮将 tiny 层常数接口改为 `\vartheta(A_1r)`，并明确目标参数 `C_Cheb=1.04,A_1=16,S_0=12,c_0=1/199.68`。A.4 同时补入 `A_1=16,A_2=1024` 的 near 层复核：素数倒数和保持绝对有界，因此只消耗 `C_USC^{lin}` 的固定余量。tiny 端点项也改成两个显式初等不等式：行方向 `64\log P≤P^{5/12}`，列方向 `256\log P≤P^{11/12}`。当前仍不能宣称无条件最终闭合，因为还必须引用或证明显式 `\vartheta(x)≤1.04x` 的适用阈值，并把低于阈值的小范围纳入有限验证。

## `C_Cheb=1.04` 安全路线复核

本轮修正了 tiny 参数目标：不再要求全局 `\vartheta(x)≤x`，而采用可审查的 `\vartheta(x)≤1.04x` 安全路线。阈值脚本新增 `--cheb-target-safe`，显示 `C_Cheb=1.04,A_1=16,S_0=12` 时机械阈值仍为 `log P≈1071.40`。新增有限核验脚本 `experiments/check_theta_cheb.py`，已验证 `2≤x≤2,000,000` 上 `\vartheta(x)≤1.04x`，最大比值约 `0.999583598860`。下一步需补正式显式解析来源或扩大有限表格，随后转攻低阶矩常数 `C_low`。

## Dusart theta 界闭合 Chebyshev 常数

本轮查明 tiny/Chebyshev 常数可无条件闭合：Dusart 的显式估计给出 `\vartheta(x)-x<x/36260` 对 `x>0` 成立。因此 `C_Cheb=1.04` 不只是安全目标，而可作为无条件默认；更锋利地可取 `C_Cheb=1+1/36260<1.000028`。阈值试算显示二者在当前低阶矩瓶颈下同样给出 `log P≈1071.40`，所以下一步真正瓶颈转为 `C_low` 的压缩。

## C_low=20 压缩进展

本轮在 A.7.0 后补入压缩版低阶矩账本：二点差分链用标准 `d_4` 平均 `\sum_{n≤X}d_4(n)\ll X\log^3X`，安全放大到 `\log^{20}P`；四点 connected 块在 `r=4` 时只有三条骨架边，非骨架额外 rank 由二阶 Euler 和吸收，不再需要 `\log^{80}P`。因此低阶矩专用常数可从 `C_low=40` 压到 `C_low=20`，对应机械阈值从 `log P≈1071.40` 降到约 `593.42`。下一步若继续压到 `10`，需要把二点安全放大从 `\log^{20}P` 再降至约 `\log^{10}P`，并更精确核算四点有限类型数。

## 当前最新阈值状态

采用 Dusart tiny 参数 `C_Cheb=1+1/36260,A_1=16,S_0=12` 与压缩低阶矩 `C_low=20` 后，阈值计算器当前输出 `log P_tail≈593.418328118502`。旧段落中的 `1071.40` 是 `C_low=40` 时的状态，保留作历史记录；当前剩余优化瓶颈是继续压 `C_low` 和显式化 `E_row(P),E_col(P)`。

## C_low=10 锋利账本接口

本轮进一步补入 `C_low=10` 的可证版本：二点链保留标准 `d_4` 平均的真实 `\log^3P`，将绝对常数放入显式闸门 `10^2\log^7P≥C_{d4}`；四点 connected 块只保留三条骨架边的 `\log^3P`，有限类型、叶剥离和非骨架二阶 Euler 尾并入常数 `C_{4,loc}`，由 `10^4\log^7P≥C_{4,loc}` 吸收。这样低阶矩形式升级为 `C_low=10`，机械阈值预计降到 `log P≈484.04`。剩余审查义务是给出 `C_{d4}` 与 `C_{4,loc}` 的显式数值，而不是再寻找新的 connected 图型。

## 当前最新阈值状态：`C_low=10`

阈值计算器当前模板已采用 `C_low=10`，输出 `log P_tail≈484.03986414399714`。该改进依赖两个有限常数闸门 `C_{d4}` 与 `C_{4,loc}`，因此审稿状态是“结构闭合、常数待赋值”。若保持完全已赋值版本，则可退回 `C_low=20` 与 `log P≈593.42`；若完成两个闸门赋值，则可正式采用 `C_low=10`。

## C_low=10 闸门数值化

本轮给出两个保守显式闸门赋值：`C_{d4}=10^4`，`C_{4,loc}=10^8`。对应初等条件分别为 `10^2\log^7P≥10^4` 与 `10^4\log^7P≥10^8`，即 `\log P` 只需大于约 `2` 与 `4`，在当前尾界阈值 `log P≈484.04` 处自动成立。阈值脚本已加入 `C_d4_gate`、`C_4loc_gate` 两项并检查余量。因此 `C_low=10` 可从“待数值化接口”升级为“保守可赋值常数”，剩余全局障碍回到 `E_row(P),E_col(P)` 与最终有限验证。

## `E_row/E_col` 闸门接口进展

本轮发现并修正列主量接口中的 `+P` 问题：第 `P` 列是排除列，不是每个允许列的损失。修正后列条件为 `E_col(P)≤P/(8\log P)`。同时 7.5 新增对数幂误差模型，模板取 `C_row_err=C_col_err=10^12,A_row_err=A_col_err=8`；阈值脚本检查显示当前 `log P≈484.04` 处行列误差闸门余量约 `6.23e18`。因此若 B.0.4\* 最终常数不超过该模板，行列主量常数可正式闭合。

## B.0.4\* 常数账本接口

本轮新增 6.15.5，把 sawtooth/DBA/coarea/高度传播损失拆成七个原子指数并保守赋值，得到 `C_*=160`。阈值脚本新增 `C_ht,C_rk_star,C_dy,C_co,C_st,C_LV,C_AE,B_fourier`，派生 `B1=960,B2=880,B4=820,B5=104`，四个参数余量均为正。于是 B.0.4\* 的数值接口已与 7.5 的行列误差闸门对齐；剩余审稿重点是坏层生成元覆盖清单，而不是阈值账本缺项。

## 坏层覆盖闭包 2.0

本轮新增 6.14.2c，将原始端点、LV/AE 中间失败项和 resultant atlas 合并为统一覆盖矩阵。该矩阵显示：除 Fourier 截断尾、端点线性组合、对角/半对角和低体积盒这些已单独计数项外，所有失败方式均进入六类 DBA 坏层。由此剩余审稿点进一步压缩为逐行正规化公式核对，而不是寻找新的坏层类型。

## `b(a)` Admissibility 闭合

本轮新增 B.0.3e+，把叶剥离剩余类严格写为 `p_*≡b(a) mod Q`，并说明正常分片上 `b(a)` 是有限复杂度 CRT 有理函数。失败情形只有三类：分母不可逆、CRT 不兼容、分支/compatibility determinant 退化，分别进入分母/共振层、零贡献、ramification/resultant 层。由此 6.14.2c 逐行复核表中的最小剩余项已闭合。

## 当前已脚本化最终账本

`experiments/explicit_threshold_ledger.py --final-ledger` 现在可输出当前已脚本化闸门的合并账本，得到 `log_P_scripted≈484.03986414399714`。该输出同时列出未纳入项，明确它不是最终“所有奇素数”的验证阈值。下一步最优方向不是继续局部压常数，而是处理三个全局定稿问题：Mertens/安全间隙/列容量的剩余 `o(1)` 阈值、有限验证不可执行性、历史追加段落清理。
